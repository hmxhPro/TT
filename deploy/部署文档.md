# SOD 智能小目标检测系统 — 离线（无网络）本地部署文档

> 适用场景：将本系统部署到**无法访问互联网**的内网 / 离线服务器。
> 部署方式：在“源机”（已配好环境的开发机）上构建**自包含离线总包**，拷贝到目标服务器解包安装，全程**不需要联网**。
> 本文档随总包一起分发（位于总包根目录 `部署文档.md`）。

---

## 目录
1. [系统架构与组件](#1-系统架构与组件)
2. [目标服务器前置要求](#2-目标服务器前置要求)
3. [离线总包内容清单](#3-离线总包内容清单)
4. [快速开始（一键安装）](#4-快速开始一键安装)
5. [详细安装步骤](#5-详细安装步骤)
6. [验证与冒烟测试](#6-验证与冒烟测试)
7. [离线部署关键点与常见坑](#7-离线部署关键点与常见坑必读)
8. [可选组件（GroundingDINO / Florence-2）](#8-可选组件)
9. [运维：启停 / 日志 / 备份 / 升级](#9-运维启停--日志--备份--升级)
10. [故障排查 FAQ](#10-故障排查-faq)
- [附录 A：目录结构与端口](#附录-a目标机目录结构与端口)
- [附录 B：在源机上重建总包](#附录-b在源机上重建离线总包)

---

## 1. 系统架构与组件

| 层 | 技术 | 说明 |
|---|---|---|
| 前端 | React 18 + Vite 5 | 单页应用（SPA），构建产物为纯静态文件 `frontend/dist` |
| 后端 | FastAPI + uvicorn（单 worker，端口 **8000**） | 提供检测 / 训练 / 历史等 REST + SSE 接口 |
| 默认检测器 | **YOLOE**（开放词表）+ **SAHI** 小目标切片 | 默认 `DETECTION_MODEL=yoloe`，开箱即用 |
| 多目标跟踪 | **ByteTrack**（随包内置本地仓库） | 视频逐帧跟踪 |
| 文本编码器 | MobileCLIP（TorchScript：`mobileclip_blt.ts`） | YOLOE 文本提示所需，**离线必须本地存在** |
| 自定义训练 | Ultralytics YOLOv11（子进程） | 在线标注 → 训练自定义类别 |
| 数据库 | PostgreSQL（**可选**） | 仅用于任务/训练历史持久化；不配也能正常检测 |
| 运行环境 | conda 环境 `SOD`：Python 3.10 + **PyTorch 2.12 (CUDA 13.0)** | 通过 `conda-pack` 整体打包，**自带 CUDA 运行时**，目标机无需装 CUDA Toolkit |

**运行时数据流**：前端（浏览器）→ nginx（静态资源 + `/api` 反代）→ FastAPI:8000 →（GPU 推理 / PostgreSQL）。

> ⚠️ 本系统**强依赖 NVIDIA GPU**。打包的 PyTorch 是 CUDA 13.0 版本，目标机必须有 GPU 与匹配驱动。

---

## 2. 目标服务器前置要求

### 2.1 硬件
- **NVIDIA GPU**，显存 **≥ 12 GB**（仅检测）；**≥ 16 GB** 推荐（开放词表 + 训练，源机为 RTX 5060 Ti 16GB）。
- 架构 **x86_64**；磁盘预留 **≥ 30 GB**（运行环境约 6GB，权重约 1.5GB，外加上传/结果/数据集增长空间）。

### 2.2 操作系统
- **Linux x86_64**，建议 **Ubuntu 22.04 / 24.04**（与源机一致或更新）。
- **glibc ≥ 2.35**。源机为 Ubuntu 24.04（glibc 2.39）。
  > conda-pack 环境要求目标机的 glibc **不低于**源机。若目标系统更老（如 CentOS 7 / Ubuntu 18.04），打包环境中的部分二进制可能无法加载——此时需在目标机上按附录方式重建环境。`verify.sh` 会即时暴露此类问题。

### 2.3 NVIDIA 驱动（关键）
- 安装 **NVIDIA 驱动 R580 及以上**（CUDA 13.0 运行时所需，≥ 580.65；源机为 580.95）。
- `nvidia-smi` 能正常输出、且显示 `CUDA Version: 13.0` 或更高。
- **无需**安装 CUDA Toolkit / cuDNN —— 这些运行时库已随 PyTorch 一并打包在 conda 环境内（`nvidia-*` pip 包）。

### 2.4 系统软件
| 软件 | 是否必需 | 用途 | 离线获取 |
|---|---|---|---|
| NVIDIA 驱动 | **必需** | GPU 推理 | 厂商 `.run` 包或系统自带 |
| `libgl1`, `libglib2.0-0` | 强烈建议 | OpenCV 运行库（`libGL.so.1` 等） | 联网机 `apt-get download` 下 `.deb` 拷入 |
| PostgreSQL 14+ | 可选 | 任务历史持久化 | `.deb` 离线包；不装则历史功能关闭 |
| nginx | 可选（推荐） | 前端静态服务 + `/api` 反代 | `.deb` 离线包；或用内置 Python 静态服务替代 |

- **ffmpeg：无需单独安装**。OpenCV（`opencv-python`）已内置 FFMPEG 后端用于视频解码。
- 端口：**8000**（后端）、**80**（nginx 前端，可改）、**5432**（PostgreSQL，仅本机）。请确认未被占用、防火墙放行 80（或你选定的前端端口）。

> 在联网机准备系统 `.deb` 的示例（拷到目标机后 `sudo dpkg -i *.deb`）：
> ```bash
> apt-get download libgl1 libglib2.0-0
> # 如需 PostgreSQL / nginx，连同其依赖一起下载：
> #   apt-get install --download-only postgresql nginx
> #   （从 /var/cache/apt/archives/ 收集 .deb）
> ```

---

## 3. 离线总包内容清单

总包 `sod-offline-bundle/`（单文件形式 `sod-offline-bundle.tar`，约 **4.5–5 GB**）：

```
sod-offline-bundle/
├── 部署文档.md                ← 本文档
├── install.sh                 ← 目标机一键安装脚本
├── verify.sh                  ← 安装后自检脚本
├── run_backend.sh             ← 非 systemd 启动脚本
├── env.template               ← .env 配置模板（含 __SOD_HOME__ 占位）
├── nginx-sod.conf             ← nginx 反向代理配置
├── sod-backend.service        ← systemd 服务单元
├── checksums.sha256           ← 各大文件校验和
├── conda/
│   └── SOD_env.tar.gz         ← conda-pack 运行环境（≈3.1 GB，含 torch+cu130 与全部依赖）
├── app/
│   └── SOD-app.tar.gz         ← 应用代码 + 模型权重（≈1.5 GB）
│        ├ backend/            （app/ 业务代码、models/ 权重、mobileclip_blt.ts、ByteTrack/、GroundingDINO/、YOLOWorld/…）
│        └ frontend/           （dist/ 构建产物 + node_modules + 源码）
└── ultralytics/
    └── Ultralytics.tar.gz     ← Arial 字体（含中文 Arial.Unicode.ttf）+ 配置（≈23 MB）
```

**已打包**：运行环境、全部模型权重（YOLOE / YOLO11 / GroundingDINO / mobileclip）、标注字体、前端构建产物、部署脚本。
**未打包（按需在目标机产生）**：上传视频 `uploads/`、检测结果 `results/`、训练产物 `runs/`、数据集 `datasets/`、标注 `annotations/`、日志 `logs/`——这些是用户运行时数据，体积大且无需迁移。

---

## 4. 快速开始（一键安装）

```bash
# ① 在目标服务器上，将总包拷贝到目标目录（如 /data）并解开
tar -xf sod-offline-bundle.tar -C /data
cd /data/sod-offline-bundle

# ② 一键安装（基础：解包环境/代码/权重 + 生成配置）
bash install.sh

# ③ 自检
bash verify.sh

# ④ 启动后端（非 systemd 方式）
nohup bash run_backend.sh > backend.out 2>&1 &

# ⑤ 浏览器访问后端文档确认： http://<服务器IP>:8000/docs
```

**全自动（需 sudo，含数据库 + nginx + 开机自启）**：
```bash
bash install.sh --with-db --with-nginx --with-systemd
bash verify.sh
# 访问： http://<服务器IP>/
```

> 安装目录默认是“总包所在目录”。如需安装到别处，用 `bash install.sh --home /opt/sod`。

---

## 5. 详细安装步骤

> 以下分步说明 `install.sh` 自动完成的工作，便于手动操作或排障。约定安装根目录为 `$SOD_HOME`。

### 5.1 传输与解包
```bash
# 源机：单文件总包便于传输
scp dist/sod-offline-bundle.tar user@目标IP:/data/
# 目标机：
cd /data && tar -xf sod-offline-bundle.tar && cd sod-offline-bundle
export SOD_HOME=$(pwd)        # 本例直接以总包目录为安装根目录
```

### 5.2 解包 Python 运行环境（conda-pack → conda-unpack）
```bash
mkdir -p "$SOD_HOME/env"
tar -xzf conda/SOD_env.tar.gz -C "$SOD_HOME/env"
source "$SOD_HOME/env/bin/activate"
conda-unpack          # 关键：修正环境内所有写死的路径前缀，使其指向新位置
source "$SOD_HOME/env/bin/deactivate"
```
> `conda-unpack` 由 conda-pack 随环境一并打入 `env/bin/`，**目标机无需安装 conda**。环境是完全自包含的。

### 5.3 解包应用代码与模型权重
```bash
tar -xzf app/SOD-app.tar.gz -C "$SOD_HOME"     # 得到 $SOD_HOME/backend 与 $SOD_HOME/frontend
# 修正历史遗留的损坏软链（指向真实的 TorchScript 文本编码器）
ln -sfn ../mobileclip_blt.ts "$SOD_HOME/backend/weights/mobileclip_blt.ts"
```

### 5.4 部署 Ultralytics 字体
```bash
mkdir -p "$SOD_HOME/.config/Ultralytics"
tar -xzf ultralytics/Ultralytics.tar.gz -C "$SOD_HOME/.config/Ultralytics"
```
> 离线模式下 ultralytics **不会**联网下载字体；`Arial.Unicode.ttf` 缺失会导致**中文标签**渲染失败。运行时通过环境变量 `YOLO_CONFIG_DIR=$SOD_HOME/.config` 指向配置根（已写入启动脚本/服务单元）。
> **注意**：ultralytics 会在 `YOLO_CONFIG_DIR` 下再建一层 `Ultralytics/` 子目录，因此字体的实际路径是 `$SOD_HOME/.config/Ultralytics/Arial.ttf`——务必放在该子目录内，否则离线 `check_font` 找不到字体。

### 5.5 生成 `backend/.env`
```bash
sed "s|__SOD_HOME__|$SOD_HOME|g" env.template > "$SOD_HOME/backend/.env"
chmod 600 "$SOD_HOME/backend/.env"
```
模板已把两处**机器相关的绝对路径**指向本机：
- `YOLOE_BASE_MODEL=$SOD_HOME/backend/models/yolo/yoloe-11l-seg.pt`
- `TRAIN_BASE_MODEL=$SOD_HOME/backend/models/yolo/yolo11l.pt`

`DATABASE_URL` 由下一步的 `init_postgres.sh` 自动写入（若不启用数据库，可保持默认；后端会在 DB 不可用时自动降级，仅关闭历史功能）。

### 5.6 （可选）初始化 PostgreSQL
前提：目标机已安装并启动 PostgreSQL，当前用户可 `sudo`。
```bash
cd "$SOD_HOME/backend"
bash scripts/init_postgres.sh      # 创建角色 sod_app（随机密码）+ 数据库 sod，并写回 .env 的 DATABASE_URL
```
> 数据表由后端启动时 `init_db()` 自动创建（`Base.metadata.create_all`），**无需手动建表 / 迁移**。

### 5.7 启动后端
**方式 A — systemd（推荐，开机自启 + 优雅停机）**
```bash
sudo sed -e "s|__SOD_HOME__|$SOD_HOME|g" -e "s|__SOD_USER__|$USER|g" \
     sod-backend.service | sudo tee /etc/systemd/system/sod-backend.service
sudo systemctl daemon-reload && sudo systemctl enable --now sod-backend
systemctl status sod-backend          # 查看状态
journalctl -u sod-backend -f          # 跟踪日志
```
**方式 B — nohup（无 root / 无 systemd）**
```bash
nohup bash "$SOD_HOME/run_backend.sh" > "$SOD_HOME/backend.out" 2>&1 &
tail -f "$SOD_HOME/backend.out"
```
> 两种方式都会：① 激活 conda 环境；② 导出离线环境变量（见第 7 节）；③ **从 `backend/` 目录**启动（相对路径 `./uploads`、`./results` 与 `mobileclip_blt.ts` 的定位都依赖此工作目录）。

### 5.8 前端部署
前端为静态文件，构建时使用**相对 API 路径**（`VITE_API_BASE_URL` 默认空），因此推荐用**反向代理让前端与 `/api` 同源**，与服务器 IP 无关、无需重新构建。

**方式 A — nginx（推荐）**
```bash
sudo sed "s|__SOD_HOME__|$SOD_HOME|g" nginx-sod.conf | sudo tee /etc/nginx/conf.d/sod.conf
sudo nginx -t && sudo systemctl reload nginx
# 访问 http://<服务器IP>/  （nginx 监听 80，反代 /api → 127.0.0.1:8000）
```
nginx 配置已为 **SSE 流式检测**关闭代理缓冲（`proxy_buffering off`），并放宽上传体积（`client_max_body_size 4g`）与长任务超时。

**方式 B — 无 nginx：内置 Python 前端服务（推荐，离线最省事）**
总包内含 `serve_frontend.py`，用打包环境运行即可：它同时**提供前端静态文件**并把 `/api` **反向代理**到后端（已支持 SSE 流式推送与大文件上传）。前端与 `/api` 同源，无需写死服务器 IP、无需 node、无需重新构建。
```bash
cd "$SOD_HOME"
# 默认监听 8080，反代到本机 :8000 的后端；后台运行：
nohup "$SOD_HOME/env/bin/python" serve_frontend.py > frontend.out 2>&1 &
# 访问 http://<服务器IP>:8080/
# 自定义端口（80 需 root）： FRONTEND_PORT=80 "$SOD_HOME/env/bin/python" serve_frontend.py
```

**方式 C — 重新构建（仅当需要把后端地址写死进静态包时）**
若目标机已装 Node（总包内含 `frontend/node_modules`）：
```bash
cd "$SOD_HOME/frontend"
VITE_API_BASE_URL="http://<服务器IP>:8000" npm run build
# 之后用任意静态服务器托管 dist/（后端已开启 CORS=*，跨源可用）
```

---

## 6. 验证与冒烟测试

```bash
bash "$SOD_HOME/verify.sh"
```
`verify.sh` 逐项检查：conda 环境、核心依赖导入、**GPU/CUDA 可用性**、模型权重、字体、前端产物、PostgreSQL、后端 `/readyz`。

手动确认：
```bash
# 1) 环境与 GPU
"$SOD_HOME/env/bin/python" -c "import torch;print(torch.__version__, torch.cuda.is_available(), torch.cuda.get_device_name(0))"
# 2) 后端健康（启动后）
curl -s http://127.0.0.1:8000/readyz        # 期望 {"db":"ok"/"loading","model":"ok","gpu":"ok"}
curl -s http://127.0.0.1:8000/health
# 3) 浏览器打开前端，上传一段短视频，输入要检测的目标（如“汽车”），观察实时检测帧推送
```
> 首次加载 YOLOE 模型需数秒到数十秒（预热）。`/readyz` 中 `model:"ok"` 表示已就绪；`model:"loading"` 或返回 503 表示仍在加载或加载失败（查日志）。

---

## 7. 离线部署关键点与常见坑（必读）

1. **离线环境变量必须以“真实环境变量”导出**（不能只写在 `.env`）。
   应用不会把 `.env` 里的 `YOLO_OFFLINE` / `ULTRALYTICS_OFFLINE` / `HF_*_OFFLINE` 注入进程环境，因此 `run_backend.sh` 与 systemd 单元里**显式 `export`** 了这些变量：
   `HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 HF_DATASETS_OFFLINE=1 YOLO_OFFLINE=1 ULTRALYTICS_OFFLINE=1`。
   否则 ultralytics 可能在启动时尝试**版本检查 / 遥测 / 资源下载**，在无网络机上**卡顿或超时**。

2. **`mobileclip_blt.ts` 必须位于后端工作目录**。YOLOE 默认文本编码器为 `mobileclip:blt`，运行时按当前工作目录查找 `mobileclip_blt.ts`（TorchScript）。务必**从 `backend/` 启动**（启动脚本已 `cd`）。该文件随包提供，请勿删除。

3. **`.env` 的两处绝对路径与本机绑定**：`YOLOE_BASE_MODEL`、`TRAIN_BASE_MODEL`。`install.sh` 已自动替换为 `$SOD_HOME` 下的真实路径；若手动改了安装目录，需同步更新这两行。

4. **标注字体不可缺**。离线下 `Arial.ttf` / `Arial.Unicode.ttf` 无法自动下载，缺失会导致检测框文字（尤其中文）渲染异常。

5. **glibc / 驱动兼容**：目标机 glibc 不低于源机、NVIDIA 驱动 R580+。`verify.sh` 的“依赖导入”和“GPU/CUDA”两项可即时暴露不兼容。

6. **OpenCV 运行库**：若导入 `cv2` 报 `libGL.so.1: cannot open shared object file`，在目标机 `sudo apt install libgl1 libglib2.0-0`（或离线 `.deb`）。

7. **数据库是可选项**：未配置 PostgreSQL 时后端照常检测，仅“历史记录”不可用（日志会提示 `DB unavailable, history persistence disabled`）。

8. **SSE 实时推流**：经反向代理时务必关闭缓冲（nginx `proxy_buffering off`，已在配置中设好），否则前端收不到逐帧检测结果。

---

## 8. 可选组件

> 默认 `yoloe` 检测器**不需要**以下组件。仅当切换检测器时才需要，且离线准备较繁琐。

### 8.1 GroundingDINO（`DETECTION_MODEL=grounding_dino`）
- 权重已随包：`backend/models/groundingdino/weights/groundingdino_swint_ogc.pth`，源码仓库在 `backend/GroundingDINO/`。
- 但 `groundingdino` Python 包**未预装**（含需编译的 CUDA 扩展）。离线启用需在目标机具备编译工具链（gcc/g++ + 与 CUDA 13.0 匹配的 nvcc）后：
  ```bash
  cd "$SOD_HOME/backend/GroundingDINO" && "$SOD_HOME/env/bin/pip" install -e . --no-build-isolation --no-index
  ```
  （还需其文本编码器 BERT 权重的本地副本。）属于**高级用法**，一般部署无需开启。

### 8.2 Florence-2（`DETECTION_MODEL=florence2`）
- 通过 HuggingFace `transformers` 加载 `microsoft/Florence-2-large`。离线需事先把该模型的 HF 缓存（`~/.cache/huggingface`）一并打包并随 `HF_HUB_OFFLINE=1` 使用。默认部署不含此权重。

---

## 9. 运维：启停 / 日志 / 备份 / 升级

**启停（systemd）**
```bash
sudo systemctl start|stop|restart sod-backend
sudo systemctl status sod-backend
journalctl -u sod-backend -f
```
**日志**：应用日志默认写 `backend/logs/`（按天切分、保留 7 天）；systemd 方式同时进 journald。

**数据目录**（运行时产生，备份/迁移时关注）：
`backend/uploads`（上传视频）、`backend/results`（检测结果帧/视频）、`backend/datasets`（训练数据集）、`backend/runs`（训练产物，含 `best.pt`）、`backend/annotations`（标注）。

**备份**：定期备份 `backend/.env`（含 DB 口令）、PostgreSQL 数据库（`pg_dump sod`）、以及需保留的 `backend/runs/*/weights/best.pt`（自定义训练模型）。

**升级代码**：替换 `backend/app` 与 `frontend/dist` 后重启后端、重载 nginx。若依赖有变更，则需在源机重做 `conda-pack` 并重新分发 `conda/SOD_env.tar.gz`。

---

## 10. 故障排查 FAQ

| 现象 | 排查 |
|---|---|
| `torch.cuda.is_available()==False` | `nvidia-smi` 是否正常；驱动是否 R580+；是否在容器内未透传 GPU |
| 启动慢 / 卡住、疑似联网 | 确认离线环境变量已 `export`（见第 7 节第 1 条）；用 systemd/`run_backend.sh` 启动而非裸 uvicorn |
| `/readyz` 返回 503，`model:"loading"` 或加载失败 | 看后端日志：权重路径是否正确（`.env` 绝对路径）、`mobileclip_blt.ts` 是否在 `backend/`、显存是否不足 |
| `ImportError: libGL.so.1` | `sudo apt install libgl1 libglib2.0-0`（或离线 `.deb`） |
| 检测框中文乱码 / 标注报字体错误 | 确认 `$SOD_HOME/.config/Ultralytics/Arial.Unicode.ttf` 存在，且 `YOLO_CONFIG_DIR` 指向 `$SOD_HOME/.config`（ultralytics 会在其下使用 `Ultralytics/` 子目录） |
| 前端能开但接口 404 / 跨域失败 | 用 nginx 同源反代（方式 A）；或重新构建时设 `VITE_API_BASE_URL` 指向后端（方式 B） |
| 实时检测帧不刷新（一次性全出/不出） | nginx 未关代理缓冲；确认 `/api/` location 有 `proxy_buffering off` |
| DB 连接失败 | `pg_isready`；`.env` 的 `DATABASE_URL` 用户名/口令是否与 `init_postgres.sh` 写入一致；不需要历史可忽略 |
| `conda-unpack: command not found` | 必须先 `source env/bin/activate` 再执行；或直接用 `env/bin/conda-unpack` |
| 解包后导入二进制报 glibc 版本错误 | 目标 glibc 低于源机：换更新系统，或按附录在目标机重建环境 |

---

## 附录 A：目标机目录结构与端口

```
$SOD_HOME/
├── env/                    conda 运行环境（已 conda-unpack）
├── backend/
│   ├── app/                FastAPI 业务代码（入口 app.main:app）
│   ├── models/yolo/        yoloe-11l-seg.pt, yolo11l.pt
│   ├── models/groundingdino/ 配置 + 权重（可选检测器）
│   ├── mobileclip_blt.ts   YOLOE 文本编码器（离线必需，CWD 定位）
│   ├── ByteTrack/ GroundingDINO/ YOLOWorld/   内置本地仓库
│   ├── scripts/init_postgres.sh
│   ├── .env                运行配置（由 env.template 生成）
│   └── uploads/ results/ runs/ datasets/ logs/   运行时自动创建
├── frontend/dist/          前端静态产物（nginx root）
├── .config/Ultralytics/    Arial 字体 + 配置（YOLO_CONFIG_DIR=$SOD_HOME/.config）
├── run_backend.sh  verify.sh  部署文档.md
```

| 端口 | 服务 | 说明 |
|---|---|---|
| 8000 | FastAPI 后端 | `0.0.0.0:8000`，被 nginx 反代 |
| 80 | nginx 前端 | 静态资源 + `/api` 反代（可改） |
| 5432 | PostgreSQL | 仅本机回环，可选 |

---

## 附录 B：在源机上重建离线总包

在已配好 `SOD` conda 环境的源机执行：
```bash
# 需先安装 conda-pack：  /home/<user>/miniconda3/bin/python -m pip install conda-pack
bash SOD/deploy/package.sh
# 产出： dist/sod-offline-bundle/  与  dist/sod-offline-bundle.tar
```
`package.sh` 会：① `conda-pack` 打包 `SOD` 环境；② 打包 `backend/`+`frontend/`+权重（排除用户数据/缓存/`.env`）；③ 打包 Ultralytics 字体；④ 复制部署脚本与本文档；⑤ 生成校验和与单文件总包。详见脚本内注释。

---

*文档版本：v1.0 ｜ 目标系统：Linux x86_64 + NVIDIA GPU（驱动 R580+，CUDA 13.0）｜ 运行环境：Python 3.10 + PyTorch 2.12（cu130）*
