# SOD 平台技术评审与修复方案

> 评审对象：视频/图像开放词汇检测 + 在线标注 + 自定义 YOLOE 训练平台
> 评审范围：自研代码 `backend/app/**`（~7200 行）、`frontend/src/**`（~4600 行）、`backend/YOLOWorld/{train_yoloe,yolo_world_detector}.py`
> 评审基础：10 维度多智能体审计（78 条发现，对抗复核确认 39 / 推翻 2）+ 人工精读核心文件
> **本次定位**：面向**公司内部人员**使用，安全/隐私/合规暂置，聚焦 **健壮性、鲁棒性、运维**

---

## 0. 技术结论

当前是一个**功能完整、单机工程打磨细致的内部原型**。即使把安全与合规完全放到一边，**仅从"内部能否长期稳定跑、出了问题能否运维"的角度看，它也还不到生产级**，主要差在三类技术问题：

1. **进程级脆弱**：所有任务状态在内存、无优雅关闭、训练子进程会成孤儿、长视频会把唯一进程吃到 OOM —— 每次重启/每个长任务都可能是一次事故。
2. **磁盘与数据无治理**：上传无上限 + 无清理 → 磁盘必满；进度不落库、DB 降级静默丢历史、删除非原子留孤儿 → 数据"对不上账"。
3. **不可运维**：无容器化/CI、无监控告警、健康检查是假探针、无备份；以及训练指标**虚高且原样展示**，会误导内部用户上线坏模型。

**做得对、不要动的地方**（详见 §9）：重计算已用 `asyncio.to_thread` 移出事件循环；训练隔离为子进程；启动时清扫残留任务；`workers=1` 是单 GPU 的正确选择；前端 API base 已走 env 变量。

---

## 1. 优先级总览

严重度：🔴Critical（内部使用也会很快出事/已在出事） · 🟠High · 🟡Medium · ⚪Low
工作量：S(<0.5d) · M(0.5–2d) · L(>2d 或需设计)

| ID | 缺陷 | 类别 | 严重度 | 工作量 | 阶段 |
|----|------|------|:--:|:--:|:--:|
| R-1 | 训练子进程在后端崩溃时成孤儿，残留占显存 | 可靠性 | 🔴 | M | P0 |
| R-2 | 无优雅关闭：关闭只打日志，不排空任务/不杀子进程/不关连接池 | 可靠性 | 🔴 | M | P0 |
| R-4 | SSE 帧队列无界，断连后持续累积 → OOM | 可靠性 | 🔴 | M | P0 |
| R-5 | 任务结果常驻内存不释放，长视频内存线性增长 | 鲁棒性 | 🔴 | M | P0 |
| R-10 | 删除/清空为破坏性操作，无确认/无审计/整目录 wipe 误删共享输出 | 可靠性 | 🔴 | S | P0 |
| P-1 | 上传无大小上限 + 无磁盘清理 → 磁盘必然写满 | 资源 | 🔴 | M | P0 |
| M-1 | val 镜像 train，mAP 虚高且原样展示给用户、无警示 | ML正确性 | 🔴 | M | P0 |
| F-3 | 标注翻页自动保存失败被静默吞掉仍翻页 → 丢标注 | 前端 | 🔴 | S | P0 |
| R-3 | 重启即丢全部在途任务，无续跑 | 可靠性 | 🟠 | L | P1 |
| R-7 | 过宽 except 静默吞错继续 | 可靠性 | 🟠 | M | P1 |
| R-8 | 模型预加载失败仅警告，之后每请求重试加载 | 可靠性 | 🟡 | S | P1 |
| R-9 | 取消训练仅 SIGTERM，无超时升级 SIGKILL | 可靠性 | 🟡 | S | P1 |
| R-11 | 检测/训练无幂等，双击/重试产生重复任务抢 GPU | 鲁棒性 | 🟠 | M | P1 |
| R-13 | base_model 任意路径仅 exists() 校验，错配晦涩失败 | 鲁棒性 | 🟡 | S | P1 |
| P-2 | image_detect 整文件读入内存，批量大图 OOM | 资源 | 🟠 | S | P1 |
| P-3 | 训练与推理争同一 cuda:0，image-detect 不计并发 | 资源 | 🟠 | M | P1 |
| P-4 | VLM 复核同步阻塞最长 60s，占 GPU 信号量名额 | 资源 | 🟡 | M | P2 |
| P-5 | DB 连接池默认参数，无 size/timeout/recycle 治理 | 资源 | 🟡 | S | P2 |
| P-6 | 无持久任务队列，编排靠进程内存（R-3 根因） | 架构 | 🟠 | L | P2 |
| P-7 | ZIP 打包/下载非流式，packaging 同步全量 | 资源 | ⚪ | M | P2 |
| P-8 | 单点无横向扩展路径 | 架构 | 🟡 | L | P2 |
| D-1 | 无迁移工具，schema 变更静默不生效 | 数据 | 🟠 | M | P1 |
| D-3 | 检测进度处理中不落库，崩溃即丢进度 | 数据 | 🟠 | S | P1 |
| D-4 | DB 降级静默丢历史，无对账重放 | 数据 | 🟠 | M | P1 |
| D-5 | 前端 localStorage 与 DB 双源发散 | 数据 | 🟡 | M | P1 |
| D-2 | 磁盘↔DB 删除非原子，孤儿无 GC | 数据 | 🟡 | M | P1 |
| D-6 | 标注保存磁盘领先 DB / 导入重复无去重 | 数据 | 🟡 | M | P2 |
| D-7 | 冻结数据集/权重与 DB 生命周期脱钩，绝对路径迁移断链 | 数据 | 🟡 | M | P2 |
| D-8 | 并发写进度无版本/条件更新 | 数据 | ⚪ | S | P2 |
| O-1 | start_all.sh 硬编码他人路径 + 无生产守护 | 运维 | 🟠 | S | P1 |
| O-2 | 无应用 Dockerfile/compose/CI | 运维 | 🟠 | L | P1 |
| O-3 | /health 浅探针，无 readiness/liveness | 运维 | 🟠 | S | P1 |
| O-4 | 磁盘无清理/保留策略（与 P-1 配套） | 运维 | 🟠 | M | P1 |
| O-5 | 零可观测：无 metrics/追踪/告警 | 运维 | 🟠 | M | P1 |
| O-9 | 无备份/灾备，数据集与权重只在单机本地盘 | 运维 | 🟠 | M | P1 |
| O-6 | 日志相对路径依赖 cwd | 运维 | 🟡 | S | P2 |
| O-7 | 权重需手工放置 + 599MB .ts 在工作树 | 运维 | 🟡 | M | P2 |
| O-8 | 生产硬件基线未定义 | 运维 | 🟡 | S | P2 |
| O-10 | 无容量/成本规划基线 | 运维 | 🟡 | M | P2 |
| O-11 | 依赖无 lockfile，构建不可复现 | 运维 | 🟡 | S | P2 |
| O-12 | 信创/国产化适配未评估 | 运维 | 🟡 | L | P2 |
| M-2 | 无模型版本治理，0 指标模型可被投产 | ML治理 | 🟠 | M | P1 |
| M-3 | 训练划分未固定种子，不可复现 | ML治理 | 🟡 | S | P1 |
| M-4 | 一键全量微调无 freeze + 评估产物未回传 | ML治理 | 🟡 | M | P2 |
| M-5 | 抽帧间重复喂旧检测给 ByteTrack，框冻结/ID 漂移 | ML正确性 | 🟡 | M | P2 |
| M-6 | 融合权重/阈值无标定，Florence-2 恒定 0.9 | ML正确性 | 🟡 | M | P2 |
| M-7 | candidate 状态机泄漏，疑似目标永不可见 | ML正确性 | 🟡 | S | P2 |
| M-8 | 标注/数据质量校验弱，退化框/空标注无治理 | ML正确性 | 🟡 | M | P2 |
| A-1 | SSE 无续传 + download 强依赖内存态，重启后 404 | API | 🟡 | M | P1 |
| A-2 | 错误体中英混杂、无结构、无错误码 | API | 🟡 | M | P1 |
| A-3 | 分页无 total + 非稳定排序 | API | 🟡 | S | P2 |
| A-5 | API 无版本前缀 | API | ⚪ | S | P2 |
| A-6 | 业务校验端点手写，OpenAPI 不自描述 | API | ⚪ | S | P2 |
| F-1 | 全应用无 ErrorBoundary，渲染异常整页白屏 | 前端 | 🟠 | S | P1 |
| F-2 | 多标签页 localStorage 互相全量覆盖 | 前端 | 🟡 | M | P1 |
| F-4 | handleRetry 用 setTimeout(30ms) 赌状态，竞态 | 前端 | 🟡 | S | P2 |
| F-5 | 阻塞式 alert + 后端 detail 原样透传 | 前端 | 🟡 | S | P2 |
| F-6 | localStorage 配额溢出静默降级 | 前端 | 🟡 | S | P2 |
| F-7 | img 无 onError，盘文件 404 显示破图 | 前端 | ⚪ | S | P2 |
| F-8 | 标注器无撤销/重做 | 前端 | 🟡 | M | P2 |
| F-9 | 无前端测试 + 生产关 sourcemap | 前端 | 🟡 | M | P2 |
| Q-1 | ~1.2 万行自研代码零自动化测试 | 质量 | 🟠 | L | P1 |
| Q-2 | 无 CI/类型门禁，lint 脚本不可用 | 质量 | 🟡 | M | P1 |
| Q-3 | config 魔法数 + 死配置项 | 质量 | 🟡 | S | P2 |
| Q-5 | detector 命名误导：yolo_world 实为 YOLOE | 质量 | ⚪ | S | P2 |
| Q-6 | README 与代码漂移 | 质量 | ⚪ | S | P2 |

> 共 **62** 项技术缺陷。**P0 = 8 项**（不修则内部使用也会出事）。

---

## 2. 健壮性与可靠性（Reliability）

### R-1 🔴 训练子进程在后端崩溃时成孤儿，残留占显存
- **现象/证据**：`training_manager.py:163` `create_subprocess_exec(...)` 未传 `start_new_session`，且后端被 SIGKILL/崩溃时不会通知子进程。监控协程 `_monitor` 随进程死亡，但 OS 层的训练子进程仍在跑、继续占满 `cuda:0`，而 DB 行被下次启动 sweep 成 `failed`（`main.py:78-86`）——状态与现实不符，显存泄漏。
- **修复**（与 R-2、R-9 合并为"子进程生命周期治理"）：
  1. 子进程独立进程组，便于整组收割：
     ```python
     proc = await asyncio.create_subprocess_exec(
         *cmd, cwd=str(_BACKEND_ROOT), env=env,
         stdout=asyncio.subprocess.PIPE, stderr=asyncio.subprocess.STDOUT,
         start_new_session=True,            # 新增：子进程独立 session/进程组
     )
     ```
  2. 启动时除了把残留行标 failed，还要按 DB 里记录的 `pid` 尝试收割上一代孤儿（`os.killpg` 前校验进程名/启动时间，避免 PID 复用误杀）。
  3. 提供 `terminate_all()` 在关闭钩子里调用（见 R-2）。

### R-2 🔴 无优雅关闭
- **现象/证据**：`main.py:108-111` lifespan `yield` 之后整个 shutdown 段只有一行 `logger.info(...)`。在途检测任务（`detect.py:162` `asyncio.create_task`）不被终止、训练子进程不被 kill、`engine` 不 `dispose()`。每次部署/重启都是硬杀。
- **修复**：在 lifespan `yield` 后补真正的收尾：
  ```python
  yield
  # ── Shutdown ──
  logger.info("Shutting down: draining tasks & killing training procs")
  # 1) 通知在途检测任务尽快收尾
  for st in task_manager.list_tasks():
      if st.status in ("pending","running","paused","packaging"):
          task_manager.request_terminate(st.task_id)
  # 2) 收割训练子进程：terminate → 等待 → kill
  await training_manager.terminate_all(grace=10)
  # 3) 释放 DB 连接池
  from app.db.session import engine
  await engine.dispose()
  ```
  `training_manager.terminate_all`：
  ```python
  async def terminate_all(self, grace: float = 10.0):
      for job_id, proc in list(self._procs.items()):
          if proc.returncode is None:
              try: os.killpg(proc.pid, signal.SIGTERM)
              except ProcessLookupError: continue
      for proc in list(self._procs.values()):
          try: await asyncio.wait_for(proc.wait(), timeout=grace)
          except asyncio.TimeoutError:
              try: os.killpg(proc.pid, signal.SIGKILL)
              except ProcessLookupError: pass
  ```
  并在 uvicorn 配置 `timeout_graceful_shutdown=30`。

### R-3 🟠 重启即丢全部在途任务，无续跑
- **现象/证据**：任务/队列/控制位全在内存（`task_manager.py:34-43`），`task_manager.py:340` 自承"内存是在途任务的真相源"。重启后只能把行标 failed（`main.py:64-91`），几十分钟的长视频任务必须重跑。
- **修复**（依赖 P-6 的持久队列为根治；轻量过渡方案）：
  1. **轻量**：每 N 帧把 `processed_frames/progress` 落库（见 D-3），重启后对 `RESULTS_DIR/{task_id}` 已落帧计数做对账，至少把"已完成进度"如实呈现，并允许"从已处理帧之后续跑"（pipeline 接受 `start_frame` 参数）。
  2. **根治**：见 P-6，把执行从 API 进程解耦到可重启的 worker，任务以 DB/Redis 为权威。

### R-4 🔴 SSE 帧队列无界，断连后持续累积 → OOM
- **现象/证据**：`task_manager.py:63` `asyncio.Queue()` 无 `maxsize`；`pipeline.py:459-463` fire-and-forget `push_frame`，无背压；`push_frame` 入队的是**未剥离 base64 的整帧**（`task_manager.py:82-85`）。用户关页不取消时，队列在 `push_done` 前一直堆积整帧 JPEG，可达 GB 级，拖垮唯一进程。
- **修复**：
  1. 队列设上限并丢最旧：
     ```python
     self._queues[task_id] = asyncio.Queue(maxsize=120)
     async def push_frame(self, task_id, frame):
         q = self._queues.get(task_id)
         if q is None: return
         if q.full():
             try: q.get_nowait()          # 丢最旧一帧，保留实时性
             except asyncio.QueueEmpty: pass
         q.put_nowait(("frame", frame))
     ```
  2. SSE 生成器取消（客户端断开）后，给一个宽限期（如 30s）无人重连则**停止入队**（用订阅引用计数：`consume_stream` 进入 +1、退出 -1，计数为 0 时 `push_frame` 直接丢弃，仅更新内存计数）。
  3. base64 缩略图改为"事件里只带 `image_filename`，前端按需 `GET /api/frame` 拉取"，从源头消除每帧 base64 推流（同时缓解 P-7/带宽）。

### R-5 🔴 任务结果常驻内存不释放
- **现象/证据**：`schemas.py:141` `results: List[FrameResult] = []`，`task_manager.py:242` 每帧 append（仅剥 base64），任务进入终态后 `_tasks` 仍长期持有（只有 history 删除才 `remove_task`，`task_manager.py:402`）。长视频 + 历史累积 → 内存单调增长，15GB 机器上 OOM。
- **修复**：
  1. `_package_zip` 已把 `results.json` 落盘（`pipeline.py:505`），终态后释放内存副本：在 `set_finished/set_early_terminated/set_failed/set_cancelled` 末尾 `state.results.clear()`，需要时从磁盘 `results.json` 重建。
  2. 给 `_tasks` 加 LRU/TTL 上限（如最多保留最近 50 个终态任务的轻量摘要，重数据按需从盘加载）。

### R-7 🟠 过宽 except 静默吞错继续
- **现象/证据**：`grep except` 统计：`dataset.py` 10 处、`training_manager.py` 9 处、`pipeline.py` 5 处等。持久化层（`task_manager.py:360-361/383-384`）、进度轮询（`training_manager.py:300-301`）等 `except Exception` 仅 `logger.warning/debug` 后继续，故障被掩盖、无计数、无告警。
- **修复**：
  1. **区分可恢复与不可恢复**：持久化失败可吞但必须计数并暴露指标（见 O-5）；pipeline 主循环里的解码/推理异常应记录到该帧结果并继续，但连续 N 帧失败应让任务 `failed` 而非无声跑完空结果。
  2. 收窄异常类型（捕获 `OperationalError`/`OSError` 而非裸 `Exception`），未预期异常让其冒泡到统一处理器（见 A-2）。
  3. 给"被吞的失败"加一个 `persist_failures_total` 计数器，超阈值告警。

### R-8 🟡 模型预加载失败仅警告，之后每请求重试加载
- **现象/证据**：`main.py:99-106` 预加载失败只 warning，注释"首个请求再加载"。若是权重缺失/GPU OOM 这类持续性故障，则**每个请求都触发一次重加载并失败**，表现为整体不可用但 `/health` 仍 ok（见 O-3）。
- **修复**：预加载失败时置一个全局 `model_ready=False` 标志；`/readyz`（O-3）据此返回 503；检测请求快速返回 503 + 明确原因，而不是每次重试昂贵加载。加载成功后置 True。

### R-9 🟡 取消训练仅 SIGTERM，无超时升级 SIGKILL
- **现象/证据**：`training_manager.py:275` `cancel()` 只 `proc.terminate()`（SIGTERM），不等待、不升级。Ultralytics + dataloader 子进程未必响应 SIGTERM，可能残留。
- **修复**：随 R-1 的 `start_new_session` 一起，cancel 改 `os.killpg(SIGTERM)` → `await wait_for(grace)` → `os.killpg(SIGKILL)`，并清理该 job 的 `runs/train/<name>` 半成品目录。

### R-10 🔴 破坏性操作无确认/无审计/整目录 wipe
- **现象/证据**：`DELETE /api/tasks`（`history.py:204`）对 `RESULTS_DIR`/`UPLOAD_DIR` 直接 `iterdir()+rmtree/unlink`（:233-243），会**误删共享的 `imgdet/` 图像检测输出**（`image_detect.py` 把结果写入 `RESULTS_DIR/imgdet/<batch_id>`，但它不以 TaskRecord 存在，不受 active 守卫保护）。即使内部使用，双击/误点即不可逆全量丢失。
- **修复**：
  1. **按 DB 行枚举删除**而非整目录 wipe：用已查出的 `deleted_ids` 逐个删 `RESULTS_DIR/{task_id}`，绝不 `iterdir` 父目录。
     ```python
     for tid in deleted_ids:
         _remove_results_dir(tid)
         # upload 仅在无其他任务引用时删
     # imgdet 等非任务目录一律不碰
     ```
  2. 破坏性端点要求显式确认参数（如 `?confirm=DELETE_ALL`），否则 409。
  3. 追加一张轻量 `operation_log`（谁/何时/删了什么/影响行数）——内部审计/排错都需要。

### R-11 🟠 检测/训练无幂等，双击/重试产生重复任务
- **现象/证据**：`detect.py:159` 每次必新建 task，`DetectRequest` 无幂等键（`schemas.py:48-82`）；upload 每次新 uuid 落盘无内容去重（`upload.py:48`）。前端双击/网络重试 → 多个并行任务抢单卡 GPU + 多份结果目录。
- **修复**：
  1. `POST /api/detect` 支持可选 `Idempotency-Key` 头；服务端对 `(video_id, 规范化 prompt/model_id, 参数)` 做去重，命中则返回既有 `task_id`。
  2. `upload` 计算内容 sha256，命中已存在则复用 `video_id`（同时省盘）。
  3. 前端提交按钮在请求 in-flight 时禁用（治标，配合后端幂等）。

### R-13 🟡 base_model 任意路径仅 exists() 校验
- **现象/证据**：`schemas.py:276` `base_model` 由请求体任意指定，`training_manager.py:100` 仅 `Path.exists()`，随后交 `YOLOE(weights)`→`torch.load`。内部场景下虽非攻击面，但**错配路径会得到晦涩的加载错误**，且 `torch.load` 默认全量反序列化不健壮。
- **修复**：
  1. `base_model` 改为按服务端预置权重的**枚举/白名单**（前端从 `GET /api/models` 选，传 `model_id`，服务端解析为受信目录内的绝对路径），而非接受任意路径字符串。
  2. 加载启用 `weights_only=True`（或 ultralytics 安全加载路径）。
  3. 路径做 `resolve()` 后 `is_relative_to(受信权重根)` 断言，错配时给出明确报错。

---

## 3. 资源与鲁棒性（Resource & Scalability）

### P-1 🔴 上传无大小上限 + 无磁盘清理 → 磁盘必满
- **现象/证据**：`upload.py:51` 注释 `Stream to disk (no size limit)`，写盘循环无字节上限；`dataset.py`/`image_detect.py` 上传同样无单文件/总量/数量限制；无任何保留期清理（见 O-4）。15GB/小盘机器上几次上传即风险。
- **修复**（上传侧 = P-1，清理侧 = O-4，配套）：
  1. 反向代理层 `client_max_body_size`；应用层在分块循环内累计字节，超 `MAX_UPLOAD_BYTES` 立即中止 + `unlink` + 返回 413：
     ```python
     MAX = settings.MAX_UPLOAD_BYTES
     while chunk := await file.read(4*1024*1024):
         total += len(chunk)
         if total > MAX:
             await out.close(); dest_path.unlink(missing_ok=True)
             raise HTTPException(413, f"文件超过上限 {MAX//1024//1024}MB")
         await out_file.write(chunk)
     ```
  2. 限制单次上传文件数；图片设 `Image.MAX_IMAGE_PIXELS` 防解压炸弹。
  3. 上传前用 `shutil.disk_usage` 检查水位，低于阈值拒绝新任务并告警。

### P-2 🟠 image_detect 整文件读入内存
- **现象/证据**：`image_detect.py:96` `raw = await f.read()` 在 `for f in files` 内对每张图整文件入内存，批量大图 OOM。
- **修复**：限制批量张数与单张大小；改流式落临时文件再解码，或解码前先校验 `Content-Length`/像素上限；解码用 `cv2.imdecode` 前限制缓冲。

### P-3 🟠 训练与推理争同一 cuda:0，image-detect 不计并发
- **现象/证据**：视频任务用 `Semaphore(2)`（`task_manager.py:332`）；训练单独 `_active_job_id` 锁（`training_manager.py:86`）；`image_detect.py:106-110` 走 `to_thread` 但**不获取任何信号量**。三者互不感知，同抢 `cuda:0`，训练 OOM 可连带打死推理。
- **修复**：
  1. 引入**统一 GPU 准入门**（一个覆盖 视频/图片/训练 的全局信号量或显存预算调度器），所有 GPU 作业先申请额度。
  2. image-detect 纳入该门控。
  3. 条件允许时把训练 `--device` 指到独立 GPU，或训练期间暂停接收新推理任务。

### P-4 🟡 VLM 复核同步阻塞最长 60s
- **现象/证据**：`vlm_service.py:54-60` 同步 `httpx.Client`，`timeout=60`；`pipeline.py:400` 在逐帧循环里同步调用，占住持有 GPU 信号量的工作线程。vLLM 卡顿时单帧最长阻塞 60s。
- **修复**：把 VLM 复核与帧推进**解耦**为独立后台消费者（复核队列），帧不等复核结果即推进，复核回来再融合更新 track；显著降低超时（如 8s）+ 熔断；vLLM 不可达时快速降级跳过复核而非每帧等连接超时。

### P-5 🟡 DB 连接池默认参数
- **现象/证据**：`session.py:23-28` `create_async_engine(..., pool_pre_ping=True)` 未设 `pool_size/max_overflow/pool_timeout/pool_recycle`。高并发或 DB 抖动时易打满、空闲连接被云库中断后复用到死连接。
- **修复**：显式配置 `pool_size=10, max_overflow=20, pool_timeout=30, pool_recycle=1800`，按压测调参。

### P-6 🟠 无持久任务队列（R-3 根因）
- **现象/证据**：`task_manager.py:10` 注释"生产应换 Celery + Redis"但未实现；`config.py:145` `REDIS_URL` 仅声明无使用；任务靠 `asyncio.create_task` 在本进程跑（`detect.py:162`）。
- **修复**（中长期根治）：引入 Arq/RQ/Celery + Redis，把"提交任务"（API，无状态可多副本）与"执行任务"（GPU worker，可独立重启）解耦；帧流改 Redis Stream/PubSub；任务状态以 DB 为权威。这一步同时根治 R-3、缓解 P-8、D-5。

### P-7 ⚪ ZIP 打包/下载非流式
- **现象/证据**：`pipeline.py:560-567` 一次性 `ZIP_DEFLATED` 全量打包；`detect.py:442` 直接 `FileResponse`。大结果集 packaging 阶段同步阻塞、下载文件名丢失中文原名。
- **修复**：打包移至线程/进程池（已部分在 to_thread，确认覆盖打包）；超大结果用 `zipstream` 边打包边下载；`Content-Disposition: filename*=UTF-8''<urlencoded 原名>`。

### P-8 🟡 单点无横向扩展路径
- **现象/证据**：`workers=1`（正确）+ 全状态内存 → 加副本会因 `_queues` 不共享导致 SSE 落到错副本 404（`detect.py:284`）。
- **修复**：属设计天花板，**先文档化**"当前为单机单副本，扩展需先做 P-6"；不要误把 `workers=1` 当 bug 去改。

---

## 4. 数据完整性与持久化（Data Integrity）

### D-1 🟠 无迁移工具，schema 变更静默不生效
- **现象/证据**：`session.py:37-43` 仅 `create_all`，注释自承"不会 ALTER 已有表，列变更需 Alembic（planned）"。一旦给已上线库加列/改类型，新代码写不存在的列会抛错并被持久化层吞掉（`task_manager.py:383`）→"历史/进度静默不写"。
- **修复**：
  1. 接入 **Alembic**：`create_all` 仅用于全新空库；后续 schema 变更走 `alembic revision --autogenerate` + `upgrade`。
  2. 启动时校验关键列存在，缺失则 **fail-fast** 并打印迁移指引，而非静默降级。
  3. CI 加 schema 漂移检测（模型与迁移不一致即失败）。

### D-2 🟡 磁盘↔DB 删除非原子，孤儿无 GC
- **现象/证据**：`history.py:169-192`、`categories.py:134-148`、`dataset.py:461-473` 均"先 commit 删行，再 best-effort 删盘"，删盘失败仅吞 `OSError`。崩在中间 → DB 无行但磁盘留 GB 级文件，无任何 GC 发现。
- **修复**：
  1. 顺序改为"先删盘成功再 commit"，或引入**两阶段标记**（`pending_delete` → 后台 reaper 真正删除并销账）。
  2. 加一个周期性**孤儿回收器**：扫描 `RESULTS_DIR/UPLOAD_DIR/datasets/runs` 与 DB 行求差集，回收无主目录（带 dry-run 与最小存活时间，避免误删在途）。

### D-3 🟠 检测进度处理中不落库，崩溃即丢进度
- **现象/证据**：`task_manager.add_frame_result_sync`（:232-244）只更新内存，DB 的 `processed_frames/progress` 只在终态写。RUNNING 期间 DB 进度长期为 0，崩溃后无法得知已处理多少。
- **修复**：节流落库（每 N 帧或每 ~3s 取一次）：
  ```python
  # pipeline 帧循环内，限频调用
  if processed % 30 == 0:
      asyncio.run_coroutine_threadsafe(
          task_manager.persist_progress(task_id, processed, total), loop)
  ```
  重启时结合磁盘已落帧数对账，支撑 R-3 的续跑。

### D-4 🟠 DB 降级静默丢历史，无对账重放
- **现象/证据**：`session.py:6-9`/`task_manager.py:383` 持久化失败全吞为 warning；`GET /api/tasks`（`history.py:80-103`）只读 DB、无内存回退。DB 抖动期间任务照跑、磁盘有结果，但历史里**凭空消失**且恢复后不补写。
- **修复**：
  1. 把"检测可用"与"持久化健康"拆成两个信号；持久化失败计数 + `/readyz` 体现 + UI 顶部提示"历史暂不可用"。
  2. 提供**对账工具**：从 `RESULTS_DIR/{task_id}/results.json` 反向重建缺失的 DB 行（一次性脚本 + 可选启动时自动补账）。
  3. 失败写入进**本地落盘的 outbox**（追加 JSONL），DB 恢复后重放。

### D-5 🟡 前端 localStorage 与 DB 双源发散
- **现象/证据**：`useDetectionTasks.js:86-88` 每次 state 变化全量写 localStorage；初始化只从 localStorage 重建，不与服务端对账。换浏览器/清缓存即任务列表消失；与 `GET /api/tasks` 不一致。
- **修复**：以服务端为单一真值，localStorage 仅作缓存；启动/刷新时拉 `/api/tasks` 做 reconcile（按 `task_id` 合并，服务端状态为准）；这一项与 F-2 一并修。

### D-6 🟡 标注保存磁盘领先 DB / 导入重复无去重
- **现象/证据**：`dataset.py:427-438` 先写 `.txt` 再 commit，commit 失败（→503）则磁盘标注领先 DB 元数据；`import_dataset` 用新 uuid，重复导入同一数据集产生重复 `DatasetImageRecord`（`filename` 无唯一约束），`image_count` 翻倍。
- **修复**：标注保存"DB 提交成功才算生效"（写临时文件 + 原子 rename，且与 DB 同事务语义对齐）；导入按内容 hash/文件名去重，给 `(category_id, content_hash)` 加唯一约束。

### D-7 🟡 冻结数据集/权重与 DB 生命周期脱钩
- **现象/证据**：`dataset_builder.finalize` 把数据集物化到 `datasets/<cat>/yolo/<job_id>/` 并把**绝对路径**写进 `dataset.yaml`（:148）；`best_pt_path/dataset_yaml` 在 DB 也是绝对路径（`training_manager.py:159/238`）。`delete_category` 不清 `runs/train` → 权重成孤儿；换机/换 CWD 后路径全断链。
- **修复**：路径以"数据根相对路径"存储，加载时拼绝对路径；删除/取消时把 `runs/train/<name>` 纳入清理或显式保留策略；提供孤儿数据集/权重扫描工具。

### D-8 ⚪ 并发写进度无版本/条件更新
- **现象/证据**：`_persist_update` 整字段覆盖（`task_manager.py:377`），`_poll_progress` 与终态 update 可竞争同行。单进程下影响小，横向扩展后会进度回退。
- **修复**：进度更新用条件写（仅当新值 > 旧值才写）或乐观版本号；文档注明当前持久化仅在单进程下正确。

---

## 5. 运维与可观测性（Ops & Observability）

### O-1 🟠 start_all.sh 硬编码他人路径 + 无生产守护
- **现象/证据**：`start_all.sh:30` `PROJECT_ROOT="/home/user/Small_object_detection/sodv3/SOD"`（真实在 `/home/hmxh/...`）、:36 vLLM venv、:43 conda.sh、:44 `sodv2` 均写死作者机路径；前端用 Vite **dev server**、靠 Ctrl+C 收尾，无守护/无重启。
- **修复**：
  1. 路径自推导：`PROJECT_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"`，外部依赖（vLLM venv/model、conda env）改环境变量带默认。
  2. 生产用 **systemd unit**（`Restart=always`、`ExecStartPre` 健康等待）或容器（见 O-2）。
  3. 前端必须 `npm run build` 出静态产物交反向代理，不能用 dev server 上线。

### O-2 🟠 无应用 Dockerfile/compose/CI
- **现象/证据**：仅 vendored 库有 Dockerfile，应用本身无 Dockerfile、无 compose、无 `.github`。部署靠手工 conda+pip 拼装，"在我机器上能跑"风险高。
- **修复**：
  1. 写多阶段 **Dockerfile**（锁定 CUDA/torch 基础镜像，固化编译依赖），`docker-compose.yml` 编排 backend + vLLM + Postgres + 前端静态服务。
  2. 建最小 **CI**（GitHub Actions / 内网 GitLab CI）：`ruff` + `mypy` + `pytest` + 前端 `eslint`/`vitest` + 构建镜像。

### O-3 🟠 /health 浅探针
- **现象/证据**：`main.py:154-160` 返回的全是静态配置，不查 DB/GPU/VLM；模型预加载失败/DB 断连仍报 ok。
- **修复**：拆 liveness 与 readiness：
  ```python
  @app.get("/livez")
  async def livez(): return {"status": "alive"}

  @app.get("/readyz")
  async def readyz():
      checks = {}
      try:
          async with AsyncSessionLocal() as s: await s.execute(text("SELECT 1"))
          checks["db"] = "ok"
      except Exception as e: checks["db"] = f"fail: {e}"
      checks["gpu"] = "ok" if torch.cuda.is_available() else "fail"
      checks["model"] = "ok" if model_ready else "loading"   # 见 R-8
      ok = all(v == "ok" for v in checks.values())
      return JSONResponse(checks, status_code=200 if ok else 503)
  ```
  编排器探 `/livez`（重启）与 `/readyz`（摘流量）。

### O-4 🟠 磁盘无清理/保留策略
- **现象/证据**：实测 `backend/uploads` 1.9G、`runs` 1.8G；`logging.py` 有日志轮转但应用产物（视频/帧/权重）无任何 TTL/水位清理。
- **修复**：后台清理 job（按 mtime + 总量）回收过期 `uploads/results/runs`；或接对象存储 + 生命周期策略；磁盘水位监控 + 高水位拒绝新任务（与 P-1 配套）。

### O-5 🟠 零可观测
- **现象/证据**：无 `/metrics`、无 prometheus_client/OpenTelemetry；`logging.py` 仅人类可读文本、无 JSON、无 request_id，跨 SSE/子进程无法串联；无 Sentry/告警。
- **修复**：
  1. 接 `prometheus-fastapi-instrumentator` 暴露 `/metrics`（请求量/时延直方图、活动任务数、GPU 显存、VLM 调用计数/错误率、持久化失败计数）。
  2. 日志改 JSON sink + 中间件注入 `request_id`（贯穿 SSE 与训练子进程日志）。
  3. 关键告警：磁盘水位、GPU OOM、任务失败率、持久化失败数。

### O-6 🟡 日志相对路径依赖 cwd
- **现象/证据**：`logging.py:32` `"logs/app_{time}.log"` + `main.py:45` `makedirs("logs")` 均相对 cwd；`backend/start.sh` 与 `start_all.sh` 两套 `logs/` 并存；容器/systemd 不同 WorkingDirectory 会落错位置甚至写不进。
- **修复**：日志根改绝对路径由 `LOG_DIR` 环境变量注入，默认 `/var/log/sod` 或挂载卷；统一启动 cwd 假设并文档化。

### O-7 🟡 权重手工放置 + 599MB .ts 在工作树
- **现象/证据**：强制离线加载（`start.sh` `HF_HUB_OFFLINE=1` 等），权重需手工下载到固定路径；仓库根有 599MB `mobileclip_blt.ts`。
- **修复**：模型获取做成带 sha256 校验的脚本/init 容器；大权重移出工作树（对象存储/Git LFS）并清理 git 历史；文档写明每个权重来源/版本/校验和。

### O-8 🟡 生产硬件基线未定义
- **现象/证据**：`config.py:181` 注释按 15GB WSL2 调 `TRAIN_DEFAULT_WORKERS=2`，而 README 推荐 RTX3090/A100，两套环境矛盾。
- **修复**：明确并文档化生产硬件基线（GPU/显存/RAM/磁盘），据此设默认并发/worker/batch；区分 dev 与 prod profile。

### O-9 🟠 无备份/灾备
- **现象/证据**：DB 只存元数据 + 路径，真正的数据集图片、标注 `.txt`、`best.pt` 全在单机本地盘；无备份脚本/异地副本/PITR。单盘损坏即永久丢失用户耗时标注与已训练模型。
- **修复**：定义 RPO/RTO；DB 定时 `pg_dump`；`datasets/runs/annotations` 定时同步到对象存储/异地（rsync/restic）；恢复演练纳入 runbook。**对内部团队，这是"丢了就回不来"的数据，建议提前到 P1 偏上。**

### O-10 🟡 无容量/成本规划基线
- **现象/证据**：`MAX_CONCURRENT_TASKS` 是唯一容量旋钮，无每用户配额、无存储配额、无"一台机能服务多少人/存多久会满"的基线。
- **修复**：压测给出单卡可承载并发与单任务显存/时延；定义每用户/部门配额与存储上限；输出容量规划表。

### O-11 🟡 依赖无 lockfile，构建不可复现
- **现象/证据**：`requirements.txt` 多处 `>=` 浮动，前端 `package.json` 全 `^`，无 `requirements.lock`/`poetry.lock`（前端有 `package-lock.json` 但后端无锁）。
- **修复**：后端用 `pip-tools`/`uv`/`poetry` 产出锁文件并提交；CI 用锁文件安装；升级走受控流程。

### O-12 🟡 信创/国产化适配未评估
- **现象/证据**：硬绑定 NVIDIA CUDA + x86 + huggingface 生态（`requirements.txt` 注释、`DEVICE=cuda`）。若客户提国产化要求需多层重做。
- **修复**：若目标客户有信创要求，前置做昇腾/海光等价推理后端可行性评估（YOLOE/MiniCPM-V 算子覆盖度）；抽象 `DEVICE`/推理后端接口，避免硬编码 cuda。

---

## 6. ML / 模型与训练正确性（ML Correctness）

### M-1 🔴 val 镜像 train，mAP 虚高且原样展示
- **现象/证据**：`dataset_builder.py:116-121` 样本不足时 `val = train`（注释自承"metrics will be optimistic"）；`val_is_train` 只进 `TrainingJobRecord.params(JSON)`，`schemas.py` 的 `TrainingJobItem/TrainedModelItem` **不含该字段**；前端 `TrainPanel.jsx:135`/`ModelListPanel.jsx:95` 直接把 `metric_map50` 当真实精度展示，零警示。实测 `runs/train/菜地_v1_cbf2f9ad/results.csv` `recall=1.0` 即过拟合信号。触发面比想象大：20% 留出后 val<5 即触发，约 **<25 张标注**就会镜像。
- **修复**：
  1. `val_is_train=True` 时**不上报 mAP**（置 None）或在 schema 增加 `val_is_train` 字段并在前端**醒目标红**："指标在训练集自测，不可作为泛化依据"。
     ```python
     # schemas.py TrainedModelItem / TrainingJobItem 增加
     val_is_train: bool = False
     # 前端 ModelListPanel：val_is_train 时 mAP 显示为 "—（训练集自测）" 红字
     ```
  2. 提高最小可训练样本门槛，强制真实留出集；样本不足时明确提示"建议至少 N 张"。
  3. 长期引入独立测试集/交叉验证。

### M-2 🟠 无模型版本治理，0 指标模型可被投产
- **现象/证据**：`runs/train` 下 7 个同名 `菜地_v1_*`，其中 `菜地_v1_440e2a7e` 跑满 64 epoch 但 mAP/precision/recall 全 0，仍产出 `best.pt`；`_next_version` 仅 `count(...)+1`（DB 不可用时恒返回 1）；检测端只校验 `weights_path` 文件存在（`detect.py:95`），全 0 模型仍可被选去做生产检测。无 active/production/rollback 概念。
- **修复**：
  1. 完成时校验**最小指标门槛**（如 mAP50 ≥ 阈值且非全 0、最少有效 epoch），未达标标 `needs_review`/`failed` 而非 `finished`，且**不进可选模型列表**：
     ```python
     if (m50 or 0) < settings.MIN_DEPLOYABLE_MAP50:
         status = "needs_review"   # 不写 TrainedModelRecord，或写但 deployable=False
     ```
  2. `TrainedModelRecord` 加 `status(active/staging/archived)` 与单调递增 version（基于 TrainingJobRecord 含失败），提供晋级/回滚接口。
  3. `run_name` 用真正递增序号或 job_id，避免同名 v1 互相覆盖语义；rc!=0/指标过低时清理 run 目录。

### M-3 🟡 训练划分未固定种子，不可复现
- **现象/证据**：`dataset_builder.py:109` `random.shuffle(items)` 用全局未播种 random，无 `random.seed`；而 Ultralytics 自身 `seed:0/deterministic:true`（args.yaml）——喂入的划分却随机。同批标注重训得到不同 split → 不同 best.pt/mAP。
- **修复**：用固定/可配置种子 `random.Random(seed).shuffle(items)`；把划分清单（train/val 的 image_id + seed + 数据版本）作为冻结产物落盘（`split.json`）并入库，使每个 best.pt 可追溯其数据划分。

### M-4 🟡 一键全量微调无 freeze + 评估产物未回传
- **现象/证据**：`train_yoloe.py:215` 全量 `model.train()`，`training_manager` 不传 `--freeze/--lr0`；小样本全量微调易过拟合。Ultralytics 已生成 `confusion_matrix.png/results.png/val_batch*.jpg` 在 run 目录，但未经 schema 回传 UI（仅 mAP50/mAP50-95 两标量）。注：零样本能力**未丧失**（开放词汇模式常驻并存），原审计"灾难性遗忘"措辞已被复核下调。
- **修复**：一键路径对小样本默认传 `--freeze`（如 backbone）+ 较小 `lr0` 并暴露给用户；把 run 目录的混淆矩阵/PR 曲线/val 可视化作为静态资源回传 UI（新增 `GET /api/training/{job}/artifacts`）；小样本时给过拟合提示。

### M-5 🟡 抽帧间重复喂旧检测给 ByteTrack
- **现象/证据**：`pipeline.py:372` 非检测帧 `raw_detections = last_raw_detections` 后 `tracker.update(...)`，把上次同一组绝对坐标框逐帧当"新观测"喂入；自适应间隔最长 30 帧（`config.py:92`）→ 框"钉"在旧位置；`fusion_engine.py:82` 每帧 `hit_count += 1`，空帧灌水使确认阈值被提前满足。
- **修复**：非检测帧让 ByteTrack 走 **Kalman 预测**推进（不重复喂旧观测），或间隔帧只预测不 `update`；`hit_count` 仅在真实检测帧累加；对快速运动场景收紧 `MAX_DETECTION_INTERVAL`。

### M-6 🟡 融合权重/阈值无标定，Florence-2 恒定 0.9
- **现象/证据**：`config.py:133-138` `VLM_WEIGHT=0.6/DINO_WEIGHT=0.4` 与一堆 0.35/0.65 阈值；`fusion_engine.py:147/163` 还散落硬编码裸数 0.60/0.50。三种检测器 score 量纲不一致（GDINO sigmoid logit、Florence-2 恒定 0.9，`detector.py:333`），却共用同一套权重阈值。
- **修复**：在留出验证集上对每个检测器分别做分数标定（Platt/温度缩放），统一概率口径再融合；阈值按检测器配置、基于 PR 曲线选点；移除 fusion 里的裸数硬编码统一走 settings。

### M-7 🟡 candidate 状态机泄漏，疑似目标永不可见
- **现象/证据**：`TrackState.status` 文档声明仅 `tentative|confirmed|rejected|lost`（`fusion_engine.py:28`），但 `apply_vlm_result` 多处置 `"candidate"`（:154/156/164/168）；`pipeline.py:600` `visible = status=="confirmed"` → candidate 永不显示；优先级逻辑也只认 confirmed/tentative。"疑似缺陷"这种最该人工复核的中间态被吞掉。
- **修复**：把 candidate 纳入状态枚举并在可见性/复核节奏/优先级中显式处理（candidate 应更频繁复核且以"疑似"呈现给用户），或明确收敛为四态之一。

### M-8 🟡 标注/数据质量校验弱
- **现象/证据**：`save_annotation`（`dataset.py:429`）直接写 cx/cy/w/h 无范围/退化校验；`get_annotation` 只 try/except 跳过坏行；`_read_label` 仅 clamp 到 [0,1] 不剔除退化框；手工标注路径无 `w/h>0` 检查（导入路径有）；空标注即背景样本，大量空标注无声稀释正样本。
- **修复**：保存与 finalize 统一做坐标范围/最小尺寸/退化框剔除与计数；产出**数据集体检报告**（每类框数、空标注占比、尺寸分布）并在训练前向用户展示与告警。

---

## 7. API 契约（API Contract）

### A-1 🟡 SSE 无续传 + download 强依赖内存态
- **现象/证据**：`stream_task` 仅内存命中才建流（`detect.py:284`），`StreamEvent` 无序号（`schemas.py:167`），前端 EventSource 无 Last-Event-ID 重放；`download_results` 也 `task_manager.get_task` miss 即 404（`detect.py:419`），**重启后已完成任务的 ZIP 在历史里点下载会 404**（盘上文件还在）。
- **修复**：
  1. `download_results` 内存 miss 时回退查 DB（`TaskRecord` + `zip_ready`）+ 磁盘 `results.zip`，使下载只依赖盘+DB（这是最小且高价值的修复）。
  2. `StreamEvent` 加单调 `seq`，响应写 `id:` 行，`event_generator` 支持 `Last-Event-ID` 从 seq 之后重放（`TaskState.results` 已有数据）。

### A-2 🟡 错误体中英混杂、无结构（含 Q-4）
- **现象/证据**：同进程内中英混杂（`detect.py:64` 英文 / `:75` 中文；`upload.py:44` 英文 / `training.py:55` 中文）；全部是 FastAPI 默认 `{"detail": <str|list>}`，校验错误是数组、业务错误是字符串；无全局 exception_handler；前端盲取 `error.response?.data?.detail`（`api.js:28`），422 时拿到 `[object Object]` 直接展示。
- **修复**：加全局异常处理器统一为 `{code, message, detail}`，错误码机读、message 统一中文；区分校验错误（字段列表）与业务错误；前端按 `code` 分支而非 message。

### A-3 🟡 分页无 total + 非稳定排序
- **现象/证据**：`list_history` 返回裸 `List`（`history.py:66-103`），无 total/has_more；排序仅 `created_at.desc()`（非唯一），同秒多任务在 offset 翻页会漏/重。training/models 列表同。
- **修复**：返回 `{items, total, limit, offset}`（total 用 `select(func.count())`）；排序加唯一第二键 `.order_by(created_at.desc(), task_id.desc())`。

### A-5 ⚪ API 无版本前缀
- **现象/证据**：全部 `prefix="/api"`（`main.py:143-151`），无 `/v1`；破坏性契约变更会同时打断所有已部署前端。
- **修复**：统一加 `/api/v1`（保留 `/api` 兼容期），破坏性变更走 `/api/v2`；前端 BASE 集中配置（已走 `VITE_API_BASE_URL`，good）。

### A-6 ⚪ 业务校验端点手写，OpenAPI 不自描述
- **现象/证据**：prompt/model_id 二选一在 schema 里都是 Optional，互斥仅在端点手写 `if bool(model_id)==bool(prompt)`（`detect.py:72`）；这些约束不出现在 OpenAPI。
- **修复**：用 pydantic `model_validator` 表达互斥/至少其一，使其进入 OpenAPI 与错误响应，`/docs` 自描述契约。

---

## 8. 前端健壮性（Frontend Robustness）

### F-1 🟠 全应用无 ErrorBoundary，渲染异常整页白屏
- **现象/证据**：`main.jsx:6-10` 仅 `<StrictMode><App/></StrictMode>`；全仓无 `ErrorBoundary/componentDidCatch`。任一渲染异常卸载整棵树 → 全白，连历史与下载入口一起消失。（注：原审计列举的几个"必崩"触发点经复核不成立——数据层已有 `JSON.parse` try/catch、可选链与 `Array.isArray` 守卫；但结构性缺口真实。）
- **修复**：顶层包 ErrorBoundary 给可重载降级 UI，并对 `TaskCard`/`KonvaAnnotator` 包局部边界，使单卡崩溃不拖垮整页：
  ```jsx
  class ErrorBoundary extends React.Component {
    state = { err: null }
    static getDerivedStateFromError(err){ return { err } }
    componentDidCatch(err, info){ console.error(err, info) /* 上报 */ }
    render(){ return this.state.err
      ? <div className="p-6">页面出错，<button onClick={()=>location.reload()}>刷新</button></div>
      : this.props.children }
  }
  // main.jsx: <ErrorBoundary><App/></ErrorBoundary>
  ```

### F-2 🟡 多标签页 localStorage 互相全量覆盖
- **现象/证据**：`taskStorage.js:12` 单 key，`saveTasks` 全量 `setItem` 覆盖；`useTrainingJobs.js:13` 同；全仓无 `storage` 事件/`BroadcastChannel`。两标签页各自整体覆盖，刷新被覆盖页 → 任务凭空消失。
- **修复**：监听 `window.addEventListener('storage', ...)` 合并/提示；或用 `BroadcastChannel` 做单写者选举；按最近写入时间戳合并而非整体覆盖；与 D-5 一并以服务端为准做 reconcile。

### F-3 🔴 标注翻页自动保存失败被静默吞掉仍翻页 → 丢标注
- **现象/证据**：`KonvaAnnotator.jsx:301-305` `goto`：`if(dirty) await handleSave(); setIdx(newIdx)` —— `setIdx` 无条件执行；`handleSave`（:278-299）catch 里只 `setError` 不抛、不返回成功标志；切图 effect 重置 boxes 丢弃未保存框。后端 DB 降级/503/超时时翻页即静默丢标注，污染训练数据源头。
- **修复**：`handleSave` 返回成功布尔，`goto` 保存失败则不翻页并提示：
  ```jsx
  const handleSave = async () => {
    try { await api.saveAnnotation(...); dirtyRef.current=false; return true }
    catch(e){ setError(`保存失败：${e.message}，未翻页`); return false }
  }
  const goto = async (newIdx) => {
    if (dirtyRef.current) { const ok = await handleSave(); if (!ok) return }
    setIdx(newIdx)
  }
  ```

### F-4 🟡 handleRetry 用 setTimeout(30ms) 赌状态
- **现象/证据**：`App.jsx:89-91` `resetOne(id); setTimeout(()=>startOne(id,...),30)`，注释"give state a tick"；`tasksRef` 由 useEffect 异步同步，30ms 不保证完成，慢设备上读到旧任务。
- **修复**：去掉 setTimeout，让"重置并立即运行"成为原子动作（以最新 file 派发并在同一回调链用最新值启动），不依赖固定延时同步 ref。

### F-5 🟡 阻塞式 alert + 后端 detail 原样透传
- **现象/证据**：`App.jsx:87` `alert(...)`；`api.js:27-31` 把后端 `detail` 原样作 `Error.message`，渲染进 `TaskCard.jsx:225`。英文/技术性文案直接暴露给非技术用户。
- **修复**：用应用内 toast/inline 替代 alert；在拦截器或展示层做技术错误→用户文案映射，仅 dev 模式保留原始 detail。

### F-6 🟡 localStorage 配额溢出静默降级
- **现象/证据**：`taskStorage.js:58-62` `saveTasks` catch 仅 `console.warn`（注释明言吞 QuotaExceeded）；无 LRU 裁剪、无大小预估。写满后此后所有持久化失效，刷新即丢全部任务且无提示。
- **修复**：持久化前估算体积并对任务做 LRU 裁剪（保留最近 N 个，终态任务只存摘要）；捕获 QuotaExceeded 时主动清理并 UI 提示"历史已满已清理"。

### F-7 ⚪ img 无 onError，盘文件 404 显示破图
- **现象/证据**：`ResultViewer.jsx`/`FramePreview.jsx` 的 `<img src>` 无 `onError`；盘文件被删/路径漂移时显示浏览器默认破图。
- **修复**：为 img 加 `onError` 切换占位图/"图片已不可用"文案；区分"无 src"与"加载失败"两种空态。

### F-8 🟡 标注器无撤销/重做
- **现象/证据**：`KonvaAnnotator.jsx` 全文无历史栈，`removeBox`/拖拽/缩放直接 setBoxes；键盘仅 Delete。标注类工具基本功能缺失，误删/误拖无法撤销。
- **修复**：引入 boxes 快照历史栈（每次 markDirty 前 push），绑定 `Ctrl+Z`/`Ctrl+Shift+Z`；至少为删除提供撤销。

### F-9 🟡 无前端测试 + 生产关 sourcemap
- **现象/证据**：无 `*.test.*`，`package.json` 无 vitest/jest 与 test 脚本；`vite.config.js:31` `sourcemap:false`。4600 行含 SSE 状态机/坐标变换/批量分组逻辑全无覆盖，线上报错栈是压缩符号。
- **修复**：对状态机与坐标/分组纯函数补 vitest 单测；生产启用 **hidden sourcemap** 并上传错误监控（Sentry）以便定位但不暴露源码。

---

## 9. 工程质量与可维护性（Quality）

### Q-1 🟠 ~1.2 万行自研代码零自动化测试
- **现象/证据**：全仓唯一 `test_yolo_world.py` 是无断言手动截图脚本；`backend/app`(7215) + `frontend/src`(4645) + 训练脚本零测试。改阈值/换模型/改坐标换算可静默回归。
- **修复**：按风险优先补测——**先覆盖最危险三块**：① 融合判定（`fusion_engine`）② 坐标/IoU/NMS 与 YOLO 标签 txt 读写 ③ `frame_quality/color_filter` 阈值。API 用 `httpx + ASGITransport` 做集成测试（detector/VLM mock）。把"能跑通一张图"升级为带 assert 的断言测试。纳入 CI（O-2）。

### Q-2 🟡 无 CI/类型门禁，lint 脚本不可用
- **现象/证据**：无 `.github/workflows`；无 mypy/ruff/eslint 配置；`package.json:10` 的 `lint` 引用的 eslint 不在 devDependencies 也未安装。
- **修复**：加 pre-commit + CI 跑 `ruff`+`mypy`（后端）、`eslint`（前端，先把 eslint 加入 devDependencies 并补 flat config 或删掉坏脚本）、`pytest`/`vitest`。

### Q-3 🟡 config 魔法数 + 死配置项
- **现象/证据**：`config.py` 200 行 40+ 阈值；`CONFIRM_THRESHOLD`/`FINAL_CONFIRM_THRESHOLD`/`REJECT_THRESHOLD`（:136-138）全仓零引用（死配置）；`BOX/TEXT_THRESHOLD` 标 Legacy 各 1 处；多组同名相近阈值仅部分生效。
- **修复**：删死配置与 Legacy 项；阈值按子系统分组（嵌套 settings）并对每个保留项注明"生效点 file:line"与语义。

### Q-5 ⚪ detector 命名误导：yolo_world 实为 YOLOE
- **现象/证据**：`detector.py:434` `elif model_name=="yolo_world"` 实际加载 `yoloe-*-seg.pt`，靠 README 反复打补丁解释；`Literal` 枚举无 `yoloe`，类名 `YOLOWorldDetector` 也不反映。
- **修复**：枚举值与类/文件改名为可区分 `yoloe`/`yolo_world` 的明确标识（或新增 `yoloe` 选项映射对应权重），使配置值/类名/实际模型三者一致，去掉文档免责注解。

### Q-6 ⚪ README 与代码漂移
- **现象/证据**：默认模型三处矛盾（`config.py:44`=grounding_dino，README=YOLOE，dev `.env`=yolo_world）；README 引用 4 个不存在的文件（`Prompt.md`/`config.js`/`package.js`/`useDetectionTask.js`，实际是 `useDetectionTasks.js`/`vite.config.js`/`package.json`）。
- **修复**：统一单一事实来源（把 `.env.example` 与 README 默认对齐为实际部署用的 `yolo_world`，或如实写 grounding_dino）；修正文件名；CI 加一个轻量脚本校验 README 中相对路径真实存在。

---

## 10. 已推翻 / 不修 / 已是正确设计（诚实标注）

| 项 | 结论 | 说明 |
|----|------|------|
| 「base64 写入 localStorage 致崩溃」 | ❌ 不成立 | `taskStorage.js` 的 `sanitize()` 已剥离 base64，仅存元数据且 allFrames 截断 300，QuotaExceeded 被 try/catch 兜住 |
| 「多类标注被静默折叠误导用户」 | ❌ 不成立 | 标注 UI 本就硬编码单类（`KonvaAnnotator.jsx:262`），且在标注/导入/API/注释四处明示单类设计；属已声明的阶段性范围 |
| 「workers=1 是 critical bug」 | ❌ 下调为设计天花板 | 单 GPU 下多 worker 只会成倍占显存；正确做法是文档化并通过 P-6 演进，而非改这行 |
| 「同步推理冻结事件循环」 | ✅ 未发生（正确设计） | 重计算已用 `asyncio.to_thread` 移出事件循环（`pipeline.py:113/156/177`），勿动 |
| 前端 API base 硬编码 | ✅ 不成立（正确设计） | 已走 `VITE_API_BASE_URL`（`api.js:11`） |

---

## 11. 分阶段实施路线图

> 内部使用语境下，**P0 = 不修则很快会出事或已在出事**；P1 = 规模化/多人使用前必修；P2 = 工程化与体验完善。

### P0（建议 1–2 周内）—— 进程不崩、磁盘不满、数据不丢、指标不骗人
- **进程稳定**：R-2 优雅关闭 + R-1 子进程不成孤儿 + R-4 SSE 队列上限 + R-5 结果内存释放（一组"进程生命周期与内存治理"PR）
- **磁盘**：P-1 上传上限 + O-4 清理 job + 水位拒绝
- **数据脚枪**：R-10 删除按行 + 二次确认 +（轻量）操作日志
- **模型可信**：M-1 虚高 mAP 标红/不上报 + M-2 的"0 指标模型不进列表"门槛
- **标注不丢**：F-3 保存失败拦截翻页

### P1（1–2 月）—— 可运维、可恢复、有回归网
- 容器化 + CI（O-2）、readiness 探针（O-3）、可观测 metrics+告警（O-5）、备份灾备（O-9）
- 迁移工具 Alembic（D-1）、进度落库（D-3）、DB 降级显式化 + 对账（D-4）、localStorage 以服务端为准（D-5/F-2）
- 幂等（R-11）、GPU 统一准入（P-3）、base_model 白名单+weights_only（R-13）、收窄异常+失败计数（R-7）、模型预加载失败 fast-fail（R-8）
- 核心三块单测（Q-1）、ErrorBoundary（F-1）、download DB 回退（A-1）、统一错误体（A-2）、训练种子（M-3）、start_all 可移植（O-1）

### P2（产品化打磨）
- 持久任务队列 / 状态外置（P-6，根治 R-3）、横向扩展（P-8）
- API 版本化（A-5）、分页 total（A-3）、撤销重做（F-8）、config 清理（Q-3）、命名与文档（Q-5/Q-6）
- 融合标定（M-6）、candidate 状态（M-7）、抽帧跟踪（M-5）、训练 freeze+评估产物（M-4）、数据体检（M-8）
- 连接池调参（P-5）、ZIP 流式（P-7）、日志绝对路径+JSON（O-6）、权重供给（O-7）、硬件基线（O-8）、容量规划（O-10）、lockfile（O-11）、信创评估（O-12）
- 前端：retry 竞态（F-4）、toast（F-5）、配额裁剪（F-6）、img onError（F-7）、前端测试+hidden sourcemap（F-9）、并发写版本（D-8）、标注/导入一致性（D-6）、数据集生命周期（D-7）

---

## 附录：本次暂置的非技术项（安全/隐私/合规，备查）

按需求本次不展开，但**对外交付前必须单独处理**，其中第 1 条建议无论如何尽快处理：

1. ⚠️ `backend/.env`（含真实 DB 口令）已被 git 跟踪且推送到公开仓库 `github.com/hmxhPro/TT.git` —— 建议立即：轮换口令 + `git rm --cached` + 加 `.gitignore` + 清洗历史 + 仓库转私有。
2. 全站无认证、CORS `*`+credentials、监听 0.0.0.0、/docs 常开。
3. 训练 `base_model` 任意路径 → `torch.load` pickle（技术修复见 R-13，安全语境为未认证 RCE）。
4. 无多租户/数据归属模型；无操作审计（技术雏形见 R-10）。
5. AGPL-3.0（ultralytics）传染 + 项目自身无 LICENSE —— 在线服务对外提供前需法务决策。
6. 数据隐私/等保 2.0/数据安全法合规面缺失。

---

*文档版本：v1.0 ｜ 评审日期：2026-06-05 ｜ 范围：健壮性/鲁棒性/运维 ｜ 共 62 项技术缺陷 + 6 项暂置安全合规项*
