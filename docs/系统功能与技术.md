# 系统功能与技术

> 本文档介绍系统的**功能、架构与技术实现**。如何安装、配置、启动与迁移见根目录 [`README.md`](../README.md)（部署文档）。

基于 **FastAPI + YOLOE + ByteTrack + React** 的视频目标检测系统：用户上传视频、用**自然语言**描述要检测的目标，系统逐帧检测、持久跟踪，并通过 SSE 把结果实时回传前端。

---

## 目录

- [核心功能](#核心功能)
- [技术方案](#技术方案)
- [检测 + 跟踪流程](#检测--跟踪流程)
- [项目目录结构](#项目目录结构)
- [各模块职责](#各模块职责)
- [大模型 (VLM) 工作原理](#大模型-vlm-工作原理)
- [任务历史持久化设计](#任务历史持久化设计)
- [训练自定义 YOLOE 模型](#训练自定义-yoloe-模型)
- [错误处理与日志](#错误处理与日志)
- [API 接口说明](#api-接口说明)
- [示例请求与返回](#示例请求与返回)
- [检测可视化效果](#检测可视化效果)
- [横向扩展](#横向扩展)

---

## 核心功能

- **自然语言开放词汇检测**：输入中文名词（如"菜地、水塘、钓鱼台"），无需预训练类别即可检测。
- **持久多目标跟踪**：ByteTrack 为每个目标分配稳定 `track_id`，跨帧不漂移。
- **每 N 帧全量检测 + 中间帧 Kalman 预测**，吞吐较逐帧检测提升约 **4~8×**。
- **VLM 语义复核**：检测得分与大模型得分按 `0.4 / 0.6` 加权融合，驱动 `tentative → confirmed / rejected` 状态机，显著降低小目标 / 罕见类误检。
- **自定义模型训练**：网页端"建类别 → 上传 → 框选标注 → 一键训练 YOLOE"，或命令行训练；产物权重可直接接回后端。
- **实时结果流 + 结果归档**：SSE 逐帧推送；任务完成打包 ZIP（标注帧 + `results.json` / `results.csv`）。
- **任务历史持久化**：PostgreSQL 落库；数据库不可用时自动降级为内存模式，不影响检测主路径。
- **多页面控制台前端**：概览 / 视频检测 / 模型训练 / 历史记录四个页面，左侧栏导航，支持**深 / 浅色主题切换**；检测任务在页面间切换时保持实时流不中断。

---

## 技术方案

| 层次 | 技术选型 | 说明 |
|------|---------|------|
| 后端框架 | **FastAPI** | 异步高性能，原生支持 SSE / WebSocket |
| 主检测模型 | **YOLOE**（Ultralytics） | 开放词汇 YOLO，速度快、显存低，**支持自定义训练** |
| 备选检测后端 | **Grounding DINO** / **Florence-2** | 同样支持开放词汇检测，按 `DETECTION_MODEL` 切换 |
| 小目标增强 | **SAHI** | 大图自动切片推理，显著提升小目标召回 |
| **大模型 (VLM)** | **MiniCPM-V**（默认）/ 任意 OpenAI 兼容模型 | **Prompt 标准化 + 检测结果语义复核**，支持本地 vLLM 与远端 API |
| 目标跟踪 | **ByteTrack** | 无需重检测的多目标跟踪，大幅降低 GPU 开销 |
| 实时推送 | **Server-Sent Events (SSE)** | 逐帧推送，前端实时刷新 |
| **任务持久化** | **PostgreSQL + SQLAlchemy 2.x (async / asyncpg)** | 历史任务、状态、进度落库；DB 不可用时降级为内存模式 |
| 前端 | **React 18 + Vite + Tailwind CSS + react-router** | 多页面控制台，HashRouter 路由，CSS 变量主题系统 |
| 部署环境 | Linux + NVIDIA GPU (CUDA) | 推荐 RTX 3090 / A100 或更高 |

> 默认检测后端为 **YOLOE**（基座权重 `yoloe-11l-seg.pt`，由 `YOLOE_BASE_MODEL` 指定），通过 `backend/.env` 的 `DETECTION_MODEL`（`yoloe` / `grounding_dino` / `florence2`）切换。大模型默认指向本地 vLLM（`http://localhost:8010/v1`，MiniCPM-V），改 `VLM_API_BASE` / `VLM_MODEL_NAME` 即可切到远端 API。配置细节见 [README](../README.md)。

### 检测 + 跟踪流程

```
视频帧序列
    │
    ├── 每隔 N 帧（默认 N=5）→ YOLOE 全量检测（GPU，可选 SAHI 切片）
    │                           ↓
    │                        ByteTrack.update(detections)  ← 分配持久 track_id
    │                           ↓
    │            （可选）VLM 语义复核：对低/中置信度 track 把裁剪图发给大模型
    │                              判断是否真为目标，与检测得分融合
    │
    └── 其余帧 → ByteTrack.update(last_detections)  ← Kalman 预测（纯 CPU）
                    ↓
              画框 + 保存 + SSE 推送
```

该策略带来约 **4~8×** 的吞吐提升（取决于 N 值与模型大小）。

> ⚙️ ByteTrack 依赖一个需编译的 `cython_bbox`（仓库已附带 `backend/ByteTrack`，安装见 [README · 安装依赖](../README.md#1-安装依赖)）。**未装好时自动降级为 passthrough 透传跟踪器**——逐帧分配新 `track_id`、无 Kalman 预测，上述持久跟踪与加速均失效（日志会打印 `Falling back to passthrough tracker`）。

---

## 项目目录结构

```
SOD/
├── backend/
│   ├── app/
│   │   ├── api/
│   │   │   ├── upload.py          # POST /api/upload（视频）
│   │   │   ├── detect.py          # POST /api/detect, GET /api/task, /stream, /download
│   │   │   ├── history.py         # 任务历史 CRUD（列表 / 帧清单 / 删除 / 清空）
│   │   │   ├── image_detect.py    # 图片批量检测
│   │   │   ├── dataset.py         # 类别图片上传 / 标注 / 数据集导入
│   │   │   ├── training.py        # 训练任务启动 / 查询 / 取消
│   │   │   └── models.py          # 已训练模型列表 / 删除
│   │   ├── core/
│   │   │   ├── config.py          # Pydantic Settings（读 .env）
│   │   │   ├── state.py           # 模型就绪等进程级状态
│   │   │   ├── errors.py          # 全局异常处理器 + request_id 中间件（统一错误体）
│   │   │   └── logging.py         # Loguru 日志 + 标准库接入 + request_id 透传
│   │   ├── db/
│   │   │   ├── session.py         # 异步引擎 + AsyncSessionLocal + init_db()
│   │   │   └── models.py          # ORM：detection_tasks / yoloe_* / operation_log
│   │   ├── models/schemas.py      # 所有 Pydantic 数据模型
│   │   ├── services/
│   │   │   ├── detector.py        # YOLOE / Grounding DINO / Florence-2 抽象
│   │   │   ├── tracker.py         # ByteTrack 封装
│   │   │   ├── visualizer.py      # 框 + 标签绘制、base64 编码
│   │   │   ├── pipeline.py        # 主视频处理流水线
│   │   │   ├── task_manager.py    # 任务注册表 + 有界 SSE 队列 + GPU 并发控制
│   │   │   ├── training_manager.py# 训练子进程生命周期（进程组、收割、取消）
│   │   │   ├── dataset_builder.py # 标注 → 冻结 YOLO 数据集（固定划分种子）
│   │   │   ├── vlm_service.py     # 大模型客户端：OpenAI 兼容协议，语义复核
│   │   │   ├── prompt_normalizer.py # 中文 prompt → 英文检测短语 + 元数据
│   │   │   ├── fusion_engine.py   # 检测分 × VLM 分加权融合，驱动 track 状态机
│   │   │   ├── color_filter.py    # 按 HSV 颜色规则降权
│   │   │   └── frame_quality.py   # 过滤低质量帧
│   │   ├── utils/video_utils.py   # 视频元数据、时间戳
│   │   └── main.py                # FastAPI 入口 + 生命周期钩子 + /livez /readyz
│   ├── YOLOWorld/                 # 自定义训练 CLI + 检测器基准（目录名为历史保留）
│   │   ├── train_yoloe.py         # 自定义训练 CLI（实为 plain YOLO11 检测微调，名称历史保留）
│   │   ├── benchmark_detectors.py # 各检测后端基准对比
│   │   ├── dataset.yaml.example
│   │   └── TRAINING.md            # 自定义训练详细文档
│   ├── scripts/init_postgres.sh   # PostgreSQL 一键初始化
│   ├── tests/                     # pytest 单元测试
│   ├── start.sh                   # 后端启动脚本（离线模式 + 优雅关闭）
│   ├── requirements.txt
│   └── .env.example
│
├── frontend/
│   ├── src/
│   │   ├── layout/
│   │   │   ├── ConsoleLayout.jsx  # 控制台外壳 + 跨页状态宿主（Outlet context）
│   │   │   ├── Sidebar.jsx        # 深/浅色侧栏导航 + 主题切换
│   │   │   └── NavItem.jsx        # NavLink 导航项
│   │   ├── pages/
│   │   │   ├── DashboardPage.jsx  # 概览：统计 + 快捷入口
│   │   │   ├── DetectPage.jsx     # 视频检测工作台
│   │   │   ├── TrainingPage.jsx   # 模型训练工作台
│   │   │   └── HistoryPage.jsx    # 历史记录
│   │   ├── components/            # VideoUploader / TaskCard / ResultViewer / FramePreview
│   │   │   │                      # CategoryManager / KonvaAnnotator / DatasetImporter
│   │   │   │                      # TrainPanel / ModelListPanel / DetectModelSelect
│   │   │   │                      # PromptInput / HistoryDetailModal / PageHeader / ui.jsx ...
│   │   ├── hooks/
│   │   │   ├── useDetectionTasks.js # 多任务上传→SSE 工作流（localStorage 持久化）
│   │   │   ├── useTrainingJobs.js   # 训练任务轮询
│   │   │   └── useTheme.js          # 深/浅色主题（localStorage + .dark 类）
│   │   ├── services/
│   │   │   ├── api.js             # Axios API 客户端
│   │   │   └── taskStorage.js     # 任务本地持久化
│   │   ├── routes.jsx             # 路由表（HashRouter）
│   │   ├── main.jsx
│   │   └── index.css             # Tailwind + CSS 变量主题令牌
│   ├── index.html
│   ├── package.json
│   ├── vite.config.js            # /api 代理到 :8000
│   └── tailwind.config.js        # brand/ink/surface 令牌（darkMode: 'class'）
│
├── docs/系统功能与技术.md         # 本文档
├── README.md                     # 部署文档
├── 技术评审与修复方案.md          # 架构审计 + 修复路线图
└── backend/{ByteTrack,GroundingDINO,YOLOWorld}/  # 第三方依赖库（含各自 README）
```

---

## 各模块职责

### 后端

| 模块 | 职责 |
|------|------|
| `app/main.py` | 创建 FastAPI 实例，注册路由与全局异常处理，启动/关闭钩子（优雅排空任务、收割训练子进程），`/livez` `/readyz` 探针 |
| `app/core/config.py` | 统一配置（`.env` 注入，多环境） |
| `app/core/errors.py` | 全局异常处理：所有非 2xx 统一为 `{code,message,detail,request_id}`；纯 ASGI request_id 中间件（写 `X-Request-ID`、贯穿日志、对 SSE 安全） |
| `app/core/logging.py` | Loguru 控制台 + 按天轮转文件日志；`InterceptHandler` 汇入 uvicorn/标准库日志；每条日志带 request_id |
| `app/api/upload.py` | 流式接收大视频，读取元数据，返回 `video_id`（带体积 / 磁盘水位上限） |
| `app/api/detect.py` | 创建检测任务、SSE 流、ZIP 下载（重启后从 DB/磁盘回退下载） |
| `app/api/history.py` | 任务历史 CRUD：分页列表 / 帧清单 / 单删 / 二次确认清空（含磁盘联动 + 审计日志） |
| `app/api/{dataset,training,models}.py` | 训练工作流：类别图片标注、数据集导入、训练任务、模型注册表 |
| `app/db/session.py` | 异步引擎与会话工厂；`init_db()` 启动自动建表 |
| `app/db/models.py` | ORM 模型（`detection_tasks` / `yoloe_*` / `operation_log`） |
| `app/services/detector.py` | 封装 YOLOE / Grounding DINO / Florence-2，统一接口（按 `DETECTION_MODEL` 选择） |
| `app/services/tracker.py` | 封装 ByteTrack，提供持久 `track_id` |
| `app/services/pipeline.py` | 主流水线：读帧→检测→跟踪→画框→推送→ZIP |
| `app/services/task_manager.py` | 任务注册表，有界 SSE 队列（丢最旧帧、哨兵不丢），GPU 并发控制，终态结果释放 + LRU |
| `app/services/training_manager.py` | 训练子进程生命周期：独立进程组、SIGTERM→SIGKILL 收割、取消、孤儿回收 |
| `app/services/vlm_service.py` | VLM 客户端：裁剪图 + 目标描述发给本地/远端 OpenAI 兼容 API，返回判定 JSON |
| `app/services/prompt_normalizer.py` | 中文自然语言 → 英文检测短语，抽取目标类型、颜色过滤等元信息 |
| `app/services/fusion_engine.py` | 检测分与 VLM 分加权融合（默认 0.4/0.6），驱动 track 状态机 |

### 前端

| 模块 | 职责 |
|------|------|
| `layout/ConsoleLayout.jsx` | 控制台外壳；唯一状态宿主，检测/训练状态经 `Outlet context` 下发，切页不卸载（SSE 流不中断） |
| `layout/Sidebar.jsx` | 侧栏导航 + 机构 logo + 深/浅色切换按钮 |
| `pages/*` | 四个页面：概览 / 视频检测 / 模型训练 / 历史记录 |
| `hooks/useDetectionTasks.js` | 多视频检测状态机（上传→任务→SSE→结果），localStorage 持久化 |
| `hooks/useTheme.js` | 主题状态：`.dark` 类切换 + localStorage 记忆 |
| `components/ResultViewer.jsx` | 实时帧大图 + 历史缩略图网格 |
| `components/KonvaAnnotator.jsx` | 在线框选标注（react-konva） |
| `services/api.js` | Axios API 客户端（同源，Vite 开发态代理到 :8000） |

---

## 大模型 (VLM) 工作原理

系统集成一个**视觉-语言大模型（VLM）**，服务于两个用途（部署与配置见 [README](../README.md#大模型-vlm-配置)）：

1. **Prompt 标准化**（`app/services/prompt_normalizer.py`）：把用户的中文自然语言（如 `"菜地、水塘"`）转换成检测器友好的英文短语（如 `"vegetable field. water pond."`），并抽取目标类型（common/small/rare）、颜色过滤规则、视觉特征等元信息，用于后续阈值调整与降权。

2. **检测结果语义复核**（`app/services/vlm_service.py` + `fusion_engine.py`）：对低/中置信度 track，把检测裁剪图发给 VLM 判断"图中是否就是用户想要的目标"，返回得分与检测器得分按 `VLM_WEIGHT` / `DINO_WEIGHT` 加权融合，驱动 `tentative → confirmed / rejected` 状态机，显著降低小目标和罕见类误检。

两个模块统一使用 **OpenAI 兼容的 Chat Completions 协议**（`POST {VLM_API_BASE}/chat/completions`），因此同一份代码既能调本地大模型也能调远端 API。

### 工作时序（简化）

```
检测器输出 box（low/mid confidence）
        │
        ▼
fusion_engine 判断该 track 是否需要复核
        │ 满足间隔条件
        ▼
vlm_service.crop_detection(frame, box)  ← 自适应扩边
        │
        ▼
POST {VLM_API_BASE}/chat/completions     ← 本地 or 远端
   payload: { image_b64, target_text }
        │
        ▼
解析返回 JSON: { is_target, matched_label, confidence, reason }
        │
        ▼
fusion_engine.apply_vlm_result(...)
   final_score = 0.4 * dino_score + 0.6 * vlm_score
   → confirmed / rejected / tentative
```

---

## 任务历史持久化设计

后端用 **PostgreSQL** 持久化任务元数据与状态，配合 SQLAlchemy 2.x 的 async ORM + `asyncpg` 驱动，全异步、不阻塞流水线。建库与配置见 [README](../README.md#postgresql-建库)。

### 设计要点

- **懒连接 + 启动不崩**：引擎在 import 时创建，连接池首次查询才建立——配置错误或 DB 不可达**不会**阻止后端启动，错误在首次查询时由持久层 `try/except` 捕获，**检测主路径不依赖 DB**。
- **DB 不可用时降级**：历史接口在 DB 失联时返回 `503`，前端仍可上传与检测，只是看不到历史；任务在内存 `task_manager` 中正常调度。
- **磁盘联动**：删除任务时同步清理 `RESULTS_DIR/{task_id}/`（帧 + ZIP）；对上传视频做引用计数，仅当无其它任务引用时才删。
- **活动任务保护**：`pending / running / paused / packaging` 的任务不可删除，必须先取消。
- **二次确认 + 审计**：全量清空需 `confirm=DELETE_ALL`，按行删除（不整目录 wipe），操作写入 `operation_log`。

### 启动时自动创建的表

| 表名 | 用途 |
|---|---|
| `detection_tasks` | 视频检测任务历史（状态 / 进度 / 时间戳） |
| `yoloe_categories` | 训练类别（draft → annotating → ready → trained） |
| `yoloe_dataset_images` | 每个类别下上传的图片 + 标注状态 |
| `yoloe_training_jobs` | 每次训练任务（进度 / 指标 / best.pt 路径 / PID） |
| `yoloe_trained_models` | 已训练模型注册表（供前端模型列表选择） |
| `operation_log` | 危险操作审计（如清空历史） |

#### `detection_tasks` 表结构

| 列 | 类型 | 说明 |
|---|---|---|
| `task_id` | `VARCHAR(36)` PK | 任务 UUID |
| `video_id` | `VARCHAR(36)` | 关联上传视频 ID |
| `video_filename` | `VARCHAR(512)` | 原始文件名（可空） |
| `prompt` | `TEXT` | 用户的自然语言检测目标 |
| `status` | `VARCHAR(32)` *(indexed)* | `pending`/`running`/`paused`/`packaging`/`finished`/`failed`/`cancelled`/`early_terminated` |
| `progress` | `FLOAT` | 0.0 – 1.0 |
| `total_frames` / `processed_frames` | `INT` | 帧计数 |
| `error` | `TEXT` | 失败原因 |
| `zip_ready` | `BOOL` | ZIP 是否可下载 |
| `early_terminated` / `termination_reason` | `BOOL` / `TEXT` | 是否提前终止及原因 |
| `created_at` *(indexed)* / `updated_at` / `finished_at` | `TIMESTAMPTZ` | 时间戳（带时区） |

> `init_db()` 的 `create_all` **只创建缺失的表，不修改已存在的表**。Schema 演进需接入 Alembic（`pip install alembic` → `alembic init` → 配置 `sqlalchemy.url` 指向 `settings.DATABASE_URL` → `alembic revision --autogenerate` → `alembic upgrade head`）。

---

## 训练自定义 YOLOE 模型

> ℹ️ **实现说明**：自定义训练实际微调的是 **plain Ultralytics YOLOv11 检测模型**（基座 `TRAIN_BASE_MODEL=yolo11l.pt`），并非 YOLOE 本身；**开放词汇检测仍使用 YOLOE**（`app/services/yoloe_detector.py`）。命令行脚本名 `train_yoloe.py` 为历史兼容保留。

支持基于自有数据集训练自定义检测模型。两种入口：**网页端工作流**（建类别→上传→框选→一键训练）和**命令行**。命令行完整说明见 [`backend/YOLOWorld/TRAINING.md`](../backend/YOLOWorld/TRAINING.md)，要点如下。

### 1. 准备数据集（YOLO 标准布局）

```
/your/dataset/
├── images/{train,val,test}/   # 图片（test 可选）
└── labels/{train,val,test}/   # 同名 .txt，每行: <class_id> <cx> <cy> <w> <h>（归一化 0–1）
```

### 2. 编写数据集 YAML

```bash
cp backend/YOLOWorld/dataset.yaml.example backend/YOLOWorld/dataset.yaml
```

```yaml
path: /your/dataset          # 数据集根目录（绝对路径）
train: images/train
val: images/val
test: images/test            # 可选
names:
  0: tower
  1: insulator
  2: vegetation
```

### 3. 启动训练

```bash
# 单卡基础训练
python backend/YOLOWorld/train_yoloe.py \
    --data backend/YOLOWorld/dataset.yaml \
    --epochs 100 --imgsz 640 --batch 16 --device cuda:0

# 多卡训练
python backend/YOLOWorld/train_yoloe.py --data backend/YOLOWorld/dataset.yaml --device 0,1 --batch 32

# 断点续训
python backend/YOLOWorld/train_yoloe.py --data backend/YOLOWorld/dataset.yaml --resume

# 冻结主干、轻量微调
python backend/YOLOWorld/train_yoloe.py --data backend/YOLOWorld/dataset.yaml --epochs 30 --freeze 10 --lr0 0.001
```

完整参数：`python backend/YOLOWorld/train_yoloe.py --help`

### 4. 训练产物 → 接回检测后端

```
backend/runs/train/yoloe_exp/weights/
├── best.pt    # 验证集最优权重 ← 部署用这个
└── last.pt    # 最后一轮（用于续训）
```

把 `best.pt` 绝对路径配置好后即可使用。网页端训练完成的模型会**自动注册**进模型列表，检测时按 `model_id` 选择该模型即可（后端走 `TrainedModelDetector` 按需加载），无需改 `.env` 或重启；命令行训练产物 `best.pt` 的注册方式见 [`TRAINING.md`](../backend/YOLOWorld/TRAINING.md)。

> ⚠️ mAP50 为 0 / 异常的模型会被标为 `needs_review` 不进入可选列表；当已标注样本过少（val 镜像 train）时，指标会被标注为"训练集自测"，不可作为泛化依据。

---

## 错误处理与日志

系统对功能失败做了**统一提示 + 服务端可追溯**：任何报错前端都能拿到结构化中文提示，后端都会留下可凭 `request_id` 检索的日志（实现见 `app/core/errors.py` 与 `app/core/logging.py`）。

### 统一错误响应

所有非 2xx 响应都是同一信封：

```json
{
  "code": "not_found",
  "message": "模型不存在。",
  "detail": null,
  "request_id": "18faeed5e947"
}
```

- `code`：机读错误码，如 `bad_request` / `not_found` / `conflict` / `unsupported_media_type` / `validation_error` / `service_unavailable` / `internal_error`。
- `message`：面向用户的中文提示（沿用各接口原有的本地化文案）。
- `detail`：附加信息——`422` 校验错误时为字段级错误列表；`500` 仅在 `DEBUG=true` 时回原始异常串，生产环境为 `null`（不泄露内部细节）。
- `request_id`：与服务端日志关联的请求 ID（见下）。

### request_id 关联

每个响应都带 `X-Request-ID` 响应头（错误体里也有同一值）。该 ID 贯穿这次请求在后端打的所有日志行，定位问题时：

```bash
grep 18faeed5e947 backend/logs/app_$(date +%F).log
```

> 反向代理可传入 `X-Request-ID` 请求头，后端会复用它，便于跨服务串联日志。

### 日志

- **位置**：控制台 + 文件 `logs/app_{YYYY-MM-DD}.log`（按天轮转、保留 7 天、历史压缩为 zip）。目录由 `LOG_DIR` 决定（解析为绝对路径，不依赖启动 cwd），级别由 `LOG_LEVEL` 决定（文件始终保留 DEBUG 留底）。
- **全量汇聚**：uvicorn / fastapi / sqlalchemy 等标准库日志经 `InterceptHandler` 汇入同一文件；**未捕获异常自动记录完整堆栈**（带该请求的 `request_id`）。
- **检测任务失败**：经 SSE `error` 事件下发到前端，并落库到 `detection_tasks.error`；流水线日志带 `[task_id]` 前缀，可凭 `task_id` 检索（见 `app/services/pipeline.py`）。

### 前端提示

`frontend/src/services/api.js` 的 Axios 拦截器把后端错误统一解析成友好中文：优先取 `message`，`422` 字段列表合并成可读文案（修正旧版显示 `[object Object]` 的问题），网络/超时给「网络连接失败」提示；`5xx` 时把 `request_id` 拼进提示便于用户上报。渲染期异常另有 `ErrorBoundary` 兜底。

---

## API 接口说明

> FastAPI 在 `http://localhost:8000/docs` 提供交互式 Swagger 文档（权威来源）。以下为常用接口摘要。**所有接口的错误响应均为统一信封**（见 [错误处理与日志](#错误处理与日志)）。
>
> 健康探针：`GET /livez`（存活）、`GET /readyz`（DB + 模型 + GPU 就绪，未就绪返回 `503` + 分项结果）、`GET /health`（浅层兼容）。

### `POST /api/upload`
上传视频（`multipart/form-data`，字段 `file`）。响应：
```json
{ "video_id": "3f4a1b2c-...", "filename": "my_video.mp4", "size_bytes": 52428800,
  "duration_seconds": 30.5, "fps": 30.0, "total_frames": 915 }
```

### `POST /api/detect`
创建检测任务（立即返回，后台异步处理）。请求体：
```json
{ "video_id": "3f4a1b2c-...", "prompt": "帮我检测视频中的菜园", "detection_interval": 5, "enable_vlm": true }
```
响应 `202`：`{ "task_id": "9a8b7c6d-...", "status": "pending", ... }`。`enable_vlm` 覆盖 `.env` 全局设置。

### `GET /api/task/{task_id}`
查询任务状态（轮询备用）：`{ "status": "running", "progress": 0.42, "processed_frames": 385, "total_frames": 915, "zip_ready": false, "results": [...] }`

### `GET /api/stream/{task_id}`
SSE 流，每处理完一帧推一条：
```
data: {"event_type":"frame","frame_result":{...},"progress":0.1,"processed_frames":10,"total_frames":100}
data: {"event_type":"done","progress":1.0,"processed_frames":100,"total_frames":100}
```

### `GET /api/download/{task_id}`
下载 ZIP（任务 `finished` 后可用）：标注帧 JPG + `results.json` + `results.csv`。

### 历史接口

| 方法 | 路径 | 说明 |
|---|---|---|
| `GET` | `/api/tasks?limit=100&offset=0&date=YYYY-MM-DD` | 分页列出历史（`created_at` 倒序，可按日期过滤）；DB 不可达返回 `503` |
| `GET` | `/api/task/{task_id}/frames` | 列出该任务保存的标注帧文件名（无目录返回 `404`） |
| `DELETE` | `/api/task/{task_id}` | 删除单任务：DB 行 + 结果目录 + ZIP + 引用计数后的上传视频（活动中返回 `409`，成功 `204`） |
| `DELETE` | `/api/tasks?confirm=DELETE_ALL` | 二次确认全量清空（缺确认串返回 `409`，有活动任务返回 `409`） |

此外还有训练工作流接口：`/api/categories`、`/api/categories/{id}/images`、`/api/categories/{id}/train`、`/api/training/jobs`、`/api/models`、`/api/image-detect` 等，详见 Swagger。

---

## 示例请求与返回

### cURL

```bash
# 1. 上传视频
VIDEO_ID=$(curl -s -X POST http://localhost:8000/api/upload -F "file=@my_garden.mp4" | jq -r '.video_id')

# 2. 开始检测
TASK_ID=$(curl -s -X POST http://localhost:8000/api/detect \
  -H "Content-Type: application/json" \
  -d "{\"video_id\":\"$VIDEO_ID\",\"prompt\":\"帮我检测视频中的菜园\"}" | jq -r '.task_id')

# 3. 订阅 SSE 流
curl -N http://localhost:8000/api/stream/$TASK_ID

# 4. 完成后下载 ZIP
curl -OJ http://localhost:8000/api/download/$TASK_ID
```

### FrameResult 示例

```json
{
  "frame_id": 125,
  "timestamp": "00:00:05.000",
  "timestamp_seconds": 5.0,
  "detections": [
    { "track_id": 3, "label": "临水菜园", "score": 0.847,
      "bbox": { "x1": 120.5, "y1": 80.2, "x2": 640.1, "y2": 350.7 } }
  ],
  "image_filename": "frame_000125_00-00-05-000.jpg",
  "image_b64": "/9j/4AAQSkZJRgAB..."
}
```

---

## 检测可视化效果

参考 `example_pro.png`，系统绘制效果：

- ✅ 高对比度矩形框（颜色随 `track_id` 轮换）
- ✅ 框上方：半透明标签背景 + 白色文字（标签 + 置信度）
- ✅ 右下角：时间戳水印（`HH:MM:SS.mmm`）
- ✅ 不同目标使用不同颜色，易于区分

---

## 横向扩展

当前设计：单 GPU，通过 `asyncio.Semaphore(MAX_CONCURRENT_TASKS)` 控制并发任务数。规模化可选以下方案。

### 多 GPU：每 GPU 一个 Worker 进程

```bash
DEVICE=cuda:0 uvicorn app.main:app --port 8000 &
DEVICE=cuda:1 uvicorn app.main:app --port 8001 &
```

```nginx
upstream backends { server 127.0.0.1:8000; server 127.0.0.1:8001; }
```

### 提升吞吐

| 优化手段 | 预期收益 |
|---------|---------|
| 增大 `DETECTION_INTERVAL`（5→10） | 推理帧数减半，速度约 2× |
| 用 `Florence-2-base` 代替 `-large` | 速度约 1.5~2×，精度稍降 |
| TensorRT 量化 Grounding DINO | 速度约 2~3× |
| NVDEC 异步解码 | 解码不占 CPU |
| 批量推理（batch > 1） | 对检测帧批量 forward |
| 半精度（fp16）推理 | 速度约 1.5×，显存减半 |

### 接入 Redis / Celery

```bash
pip install celery[redis] redis
```

```python
# app/tasks/celery_app.py
from celery import Celery
from app.core.config import settings
celery_app = Celery("video_detection", broker=settings.REDIS_URL, backend=settings.REDIS_URL)

@celery_app.task(bind=True)
def run_detection_task(self, task_id, video_path, prompt):
    ...  # 调用 pipeline
```

```bash
DEVICE=cuda:0 celery -A app.tasks.celery_app worker --concurrency=1 -n worker0@%h
DEVICE=cuda:1 celery -A app.tasks.celery_app worker --concurrency=1 -n worker1@%h
```

把 `detect.py` 中的 `asyncio.create_task(...)` 替换为 `run_detection_task.delay(task_id, str(video_path), prompt)`。

---

*构建于开源模型之上，感谢 Ultralytics（YOLOE / YOLO11）、IDEA Research（Grounding DINO）、Microsoft（Florence-2）、OpenBMB（MiniCPM-V）、ByteTrack 与 SAHI 团队。*
